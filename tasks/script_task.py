# -*- coding: utf-8 -*-
"""自定义脚本：用中文命令调用现有找图、点击、按键等能力。"""

from __future__ import annotations

import html
import logging
import re
import hashlib
from typing import Any, Dict, List, Optional, Sequence, Tuple

logger = logging.getLogger(__name__)

TASK_TYPE = "自定义脚本"
TASK_NAME = "自定义脚本"
REQUIRES_INPUT_LOCK = True
SUPPORTED_CONNECTION_TYPES = frozenset({"sequential", "success", "failure"})

DEFAULT_SCRIPT_SOURCE = ""
SCRIPT_PLACEHOLDER = "在这里写中文命令"
SCRIPT_INSERT_GROUPS = (
    {
        "title": "动作",
        "items": (
            {
                "name": "找图",
                "signature": (
                    "找图(图片, 阈值=0.8, 点击=假, 双击=假, 偏移x=0, 偏移y=0, 随机=0, 模式=无)\n"
                    "找图(图片1, 图片2, 阈值=0.8, 点击=假, 模式=无)\n"
                    "找图(图片, 区域=(x, y, 宽, 高), 模式=无)"
                ),
                "params": (
                    ("图片", "阈值=0.8", "点击=假", "双击=假", "偏移x=0", "偏移y=0", "随机=0", "模式=无"),
                    ("图片", "图片2", "阈值=0.8", "点击=假", "双击=假", "偏移x=0", "偏移y=0", "随机=0", "模式=无"),
                    ("图片", "阈值=0.8", "点击=假", "区域=(x, y, 宽, 高)", "双击=假", "偏移x=0", "偏移y=0", "随机=0", "模式=无"),
                ),
                "note": "当前窗口找图，不做缩放、灰度和旋转。多张图与卡片多图一致：并行匹配，按列表顺序取第一张命中。",
                "kind": "expr",
                "snippet": "找图(图片, 阈值=0.8, 点击=假)",
            },
            {
                "name": "点击",
                "signature": (
                    "点击()\n"
                    "点击(目标, 键=\"左键\", 双击=假, 偏移x=0, 偏移y=0, 随机=0, 模式=无)\n"
                    "点击(x, y, 键=\"左键\", 双击=假, 偏移x=0, 偏移y=0, 随机=0, 模式=无)"
                ),
                "params": (
                    (),
                    ("目标", "双击=假", "偏移x=0", "偏移y=0", "随机=0", "模式=无"),
                    ("x", "y"),
                    ("x", "y", '键="右键"'),
                    ("x", "y", "双击=假", "偏移x=0", "偏移y=0", "随机=0", "模式=无"),
                    ("x", "y", '键="右键"', "双击=假", "偏移x=0", "偏移y=0", "随机=0"),
                ),
                "note": "可点找图或检测结果：点击(图)、点击(结果, 键=\"右键\")。框内比例点用 框内点(结果, 0.5, 0.85) 或 结果.点(0.5, 0.85)。偏移是固定像素，再写随机= 就是固定偏移后再抖。文字请用 点文字()。不写参数时点上次成功位置。",
                "kind": "expr",
                "snippet": "点击(目标, 双击=假, 偏移x=0, 偏移y=0, 随机=0)",
            },
            {
                "name": "移动",
                "signature": "移动(目标, 模式=无)  /  移动(x, y, 模式=无)",
                "params": (("目标", "模式=无"), ("x", "y", "模式=无")),
                "note": "把鼠标移到检测/找图结果或坐标，不点击。",
                "snippet": "移动(x, y)",
            },
            {
                "name": "按键",
                "signature": '按键("Ctrl+C", 模式=无)  /  按键("W", 秒=0.4, 连发=真, 间隔=0.05, 模式=无)  /  按键("W", 动作="按下", 模式=无)',
                "params": (("按键", "模式=无"), ("按键", "秒=0.4", "连发=真", "间隔=0.05", "模式=无"), ("按键", '动作="按下"', "模式=无"), ("按键", '动作="松开"', "模式=无"), ("按键", "秒=(0.2, 0.6)", "连发=真", "模式=无")),
                "note": "点按一下。秒= 是按住多久再松开。连发=真 时继续出字，间隔= 是两次出字间隔秒数，省略则用系统键盘重复率。连发=假 只按住不连发。动作=按下/松开 只做一半，和鼠标同一套。",
                "snippet": "按键(按键)",
            },
            {
                "name": "输入",
                "signature": '输入("文本", 模式=无)  /  输入("文本", 方式="粘贴", 模式=无)',
                "params": (("文本", "模式=无"), ("文本", '方式="粘贴"', "模式=无")),
                "note": "往当前焦点输入文字。方式=粘贴 走剪贴板 Ctrl+V，适合长文本；默认仿真逐字输入。",
                "snippet": "输入(文本)",
            },
            {
                "name": "延时",
                "signature": "延时(秒)",
                "params": ("秒",),
                "note": "等待秒数，可被停止打断。",
                "snippet": "延时(秒)",
            },
            {
                "name": "找字",
                "signature": '找字()  /  找字(目标="金币")  /  找字(目标="金币", 区域=(x, y, 宽, 高))',
                "params": ((), ('目标="金币"',), ('目标="金币"', "区域=(x, y, 宽, 高)")),
                "note": "默认识别整个窗口。写了目标就按是否包含该文字判成功。",
                "kind": "expr",
                "snippet": "找字(目标)",
            },
            {
                "name": "找字库",
                "signature": '找字库(字库="ui.txt")  /  找字库(目标="确定", 字库="ui.txt", 颜色="ffffff-101010", 相似度=0.9, 区域=(x, y, 宽, 高))',
                "params": (
                    ('字库="ui.txt"',),
                    ('目标="确定"', '字库="ui.txt"', '颜色="ffffff-101010"', "相似度=0.9"),
                    ('目标="确定"', '字库="ui.txt"', "区域=(x, y, 宽, 高)"),
                ),
                "note": "用点阵字库识别，兼容大漠字库格式。留空目标则识别全部；颜色按大漠 RRGGBB-偏色，留空则自动二值化。",
                "kind": "expr",
                "snippet": '找字库(目标="确定", 字库="ui.txt")',
            },
            {
                "name": "点字库",
                "signature": '点字库(目标, 字库="ui.txt", 点击=真, 键="左键", 模式=无)  /  点字库(目标, 字库="ui.txt", 颜色="ffffff-101010", 相似度=0.9, 区域=(x, y, 宽, 高), 点击=真, 双击=假, 偏移x=0, 偏移y=0, 随机=0, 模式=无)',
                "params": (
                    ("目标", '字库="ui.txt"', "点击=真", '键="左键"', "模式=无"),
                    ("目标", '字库="ui.txt"', '颜色="ffffff-101010"', "相似度=0.9", "区域=(x, y, 宽, 高)", "点击=真", "双击=假", "偏移x=0", "偏移y=0", "随机=0", "模式=无"),
                ),
                "note": "先用字库找字再点击。",
                "kind": "expr",
                "snippet": '点字库(目标, 字库="ui.txt")',
            },
            {
                "name": "等字库",
                "signature": '等字库(目标="确定", 字库="ui.txt", 超时=8, 间隔=0.3)',
                "params": (('目标="确定"', '字库="ui.txt"', "超时=8", "间隔=0.3"),),
                "note": "超时秒内反复用字库找字。",
                "kind": "expr",
                "snippet": '等字库(目标="确定", 字库="ui.txt")',
            },
            {
                "name": "等字库消失",
                "signature": '等字库消失(目标="确定", 字库="ui.txt", 超时=8, 间隔=0.3)',
                "params": (('目标="确定"', '字库="ui.txt"', "超时=8", "间隔=0.3"),),
                "note": "等到字库认不到这段字。",
                "kind": "expr",
                "snippet": '等字库消失(目标="确定", 字库="ui.txt")',
            },
            {
                "name": "检测",
                "signature": '检测(模型)  /  检测(模型, 类别="敌人", 阈值=0.5, 策略="最近")  /  检测(模型, 类别="敌人", 区域=(x, y, 宽, 高))  /  检测(模型, 类别="敌人", 区域="打怪")',
                "params": (
                    ("模型",),
                    ("模型", '类别="敌人"', "阈值=0.5", '策略="最近"'),
                    ("模型", '类别="敌人"', "阈值=0.5", '策略="最近"', "区域=(x, y, 宽, 高)"),
                    ("模型", '类别="敌人"', "阈值=0.5", '策略="最近"', '区域="打怪"'),
                ),
                "note": "跟画布 YOLO 卡的「原生/插件」。模型和类别跟卡片一样，也可写成 检测(\"yolo/xxx.onnx\", 类别=\"敌人\")。区域= 可写 (x, y, 宽, 高) 或 区域.设置 登记过的名字。只识别，不点；要点写 点击(结果)。",
                "kind": "expr",
                "snippet": "检测(模型)",
            },
            {
                "name": "框内点",
                "signature": "框内点(目标, 横向=0.5, 纵向=0.5)  /  目标.点(横向, 纵向)",
                "params": (("目标", "横向=0.5", "纵向=0.5"),),
                "note": "按框算比例点，可不用。复杂落点请自己写：目标.左 + 目标.宽 * 小数，再用 整数、限制、开方、角度。",
                "kind": "expr",
                "snippet": "框内点(目标, 横向=0.5, 纵向=0.5)",
            },
            {
                "name": "随机点",
                "signature": "随机点(目标, 边距=2)  /  目标.随机点(边距)",
                "params": (("目标", "边距=2"),),
                "note": "在检测框内随机取一点，边距是离边多少像素。",
                "kind": "expr",
                "snippet": "随机点(目标, 边距=2)",
            },
            {
                "name": "找色",
                "signature": '找色(颜色, 点击=假, 模式=无)  /  找色(颜色, 区域=(x, y, 宽, 高), 双击=假, 偏移x=0, 偏移y=0, 随机=0, 模式=无)',
                "params": (
                    ("颜色", "点击=假", "双击=假", "偏移x=0", "偏移y=0", "随机=0", "模式=无"),
                    ("颜色", "点击=假", "区域=(x, y, 宽, 高)", "双击=假", "偏移x=0", "偏移y=0", "随机=0", "模式=无"),
                ),
                "note": "在当前窗口找颜色。单点写 红,绿,蓝；多点写 红,绿,蓝|偏移X,偏移Y,红,绿,蓝。工具栏「取色」可连点再完成。点击=真 时找到后立刻点。",
                "kind": "expr",
                "snippet": "找色(颜色, 点击=假)",
            },
            {
                "name": "拖拽",
                "signature": "拖拽(起点, 终点, 模式=无)  /  拖拽(x1, y1, x2, y2, 模式=无)",
                "params": (("起点", "终点", "模式=无"), ("x1", "y1", "x2", "y2", "模式=无")),
                "note": "从起点拖到终点。可传检测结果或 框内点：拖拽(结果, 框内点(结果, 0.1, 0.5))。",
                "snippet": "拖拽(x1, y1, x2, y2)",
            },
            {
                "name": "滚轮",
                "signature": '滚轮(方向="向下", 步数=3, 模式=无)  /  滚轮(目标, 方向="向下", 模式=无)  /  滚轮(步数, x, y, 模式=无)',
                "params": (('方向="向下"', "步数=3", "模式=无"), ("目标", '方向="向下"', "步数=3", "模式=无"), ("步数", "x", "y", "模式=无")),
                "note": "滚动鼠标滚轮。可先移到检测结果再滚：滚轮(结果, 方向=\"向下\")。正数或向下为往下，负数或向上为往上。",
                "kind": "stmt",
                "snippet": '滚轮(方向="向下", 步数=3)',
            },
            {
                "name": "点文字",
                "signature": '点文字(目标, 点击=真, 键="左键", 模式=无)  /  点文字(目标, 双击=假, 偏移x=0, 偏移y=0, 随机=0, 模式=无)',
                "params": (
                    ("目标", "点击=真", '键="左键"', "双击=假", "偏移x=0", "偏移y=0", "随机=0", "模式=无"),
                    ("目标", "点击=真", "区域=(x, y, 宽, 高)", "双击=假", "偏移x=0", "偏移y=0", "随机=0", "模式=无"),
                ),
                "note": "先找字再点找到的文字。偏移和随机与点击相同。",
                "kind": "expr",
                "snippet": "点文字(目标)",
            },
            {
                "name": "点元素",
                "signature": "点元素(名称=无, 点击=真, 自动化ID=无, 类名=无, 控件类型=无, 序号=0, 搜索深度=30, 超时=5, 调用=真, 按钮=\"左键\", 模式=无)",
                "params": (("名称=无", "点击=真", "调用=真", '按钮="左键"', "模式=无"), ("名称=无", "自动化ID=无", "类名=无", "控件类型=无", "序号=0", "搜索深度=30", "超时=5", "调用=真", '按钮="左键"', "模式=无")),
                "note": "按名称、AutomationId、类名或控件类型定位 UIAutomation 元素；可选择匹配序号、搜索深度、调用方式和鼠标按钮。",
                "kind": "expr",
                "snippet": "点元素(名称=\"确定\")",
            },
            {
                "name": "等图",
                "signature": "等图(图片, 超时=8, 间隔=0.3)  /  等图(图片1, 图片2, 超时=8, 间隔=0.3)",
                "params": (("图片", "超时=8", "间隔=0.3"), ("图片", "图片2", "超时=8", "间隔=0.3")),
                "note": "超时秒内反复找图，找到就返回。多图规则与找图相同。间隔是每次查找之间的等待。",
                "kind": "expr",
                "snippet": "等图(图片, 超时=8, 间隔=0.3)",
            },
            {
                "name": "等色",
                "signature": "等色(颜色, 超时=8, 间隔=0.3)",
                "params": (("颜色", "超时=8", "间隔=0.3"),),
                "note": "超时秒内反复找色。",
                "kind": "expr",
                "snippet": "等色(颜色, 超时=8, 间隔=0.3)",
            },
            {
                "name": "等文字",
                "signature": '等文字(目标="金币", 超时=8, 间隔=0.3)',
                "params": (('目标="金币"', "超时=8", "间隔=0.3"),),
                "note": "超时秒内反复找字。",
                "kind": "expr",
                "snippet": "等文字(目标, 超时=8)",
            },
            {
                "name": "等图消失",
                "signature": "等图消失(图片, 超时=8, 间隔=0.3)  /  等图消失(图片1, 图片2, 超时=8, 间隔=0.3)",
                "params": (("图片", "超时=8", "间隔=0.3"), ("图片", "图片2", "超时=8", "间隔=0.3")),
                "note": "等到这张图找不到。弹窗关掉、进度条走完用这个。",
                "kind": "expr",
                "snippet": "等图消失(图片, 超时=8, 间隔=0.3)",
            },
            {
                "name": "等色消失",
                "signature": "等色消失(颜色, 超时=8, 间隔=0.3)",
                "params": (("颜色", "超时=8", "间隔=0.3"),),
                "note": "等到这个颜色找不到。",
                "kind": "expr",
                "snippet": "等色消失(颜色, 超时=8, 间隔=0.3)",
            },
            {
                "name": "等文字消失",
                "signature": "等文字消失(目标, 超时=8, 间隔=0.3)",
                "params": (("目标", "超时=8", "间隔=0.3"),),
                "note": "等到这段字认不到。",
                "kind": "expr",
                "snippet": "等文字消失(目标, 超时=8)",
            },
            {
                "name": "等检测",
                "signature": '等检测(模型, 超时=8, 间隔=0.3)  /  等检测(模型, 类别="敌人", 阈值=0.5, 策略="最近", 区域=(x, y, 宽, 高), 超时=8, 间隔=0.3)  /  等检测(模型, 类别="敌人", 区域="打怪", 超时=8)',
                "params": (("模型", "超时=8", "间隔=0.3"), ("模型", '类别="敌人"', "阈值=0.5", '策略="最近"', "区域=(x, y, 宽, 高)", "超时=8", "间隔=0.3"), ("模型", '类别="敌人"', '区域="打怪"', "超时=8")),
                "note": "超时秒内反复检测，出现就返回结果。可再写 阈值、策略、区域；区域= 可写四元组或 区域.设置 登记过的名字。",
                "kind": "expr",
                "snippet": "等检测(模型, 超时=8, 间隔=0.3)",
            },
            {
                "name": "等检测消失",
                "signature": '等检测消失(模型, 超时=8, 间隔=0.3)  /  等检测消失(模型, 类别="敌人", 阈值=0.5, 策略="最近", 区域=(x, y, 宽, 高), 超时=8, 间隔=0.3)  /  等检测消失(模型, 类别="敌人", 区域="打怪", 超时=8)',
                "params": (("模型", "超时=8", "间隔=0.3"), ("模型", '类别="敌人"', "阈值=0.5", '策略="最近"', "区域=(x, y, 宽, 高)", "超时=8", "间隔=0.3"), ("模型", '类别="敌人"', '区域="打怪"', "超时=8")),
                "note": "等到这个类别检测不到。区域= 可写四元组或 区域.设置 登记过的名字。",
                "kind": "expr",
                "snippet": "等检测消失(模型, 超时=8, 间隔=0.3)",
            },
            {
                "name": "持续检测",
                "signature": '持续检测(模型, 间隔=0.3)  /  持续检测(模型, 类别="敌人", 阈值=0.5, 策略="最近", 区域=(x, y, 宽, 高), 间隔=0.3)  /  持续检测(模型, 类别="敌人", 区域="打怪", 间隔=0.2)',
                "params": (("模型", "间隔=0.3"), ("模型", '类别="敌人"', "阈值=0.5", '策略="最近"', "区域=(x, y, 宽, 高)", "间隔=0.3"), ("模型", '类别="敌人"', '区域="打怪"', "间隔=0.2")),
                "note": "必须写模型，路径和画布 YOLO 卡一样，例如 \"yolo/xxx.onnx\"。后台持续识别，结果写进 检测。主流程或子程序里读 检测、检测.列表，不要再写 检测()。不会自动点。同时只能一条。区域= 可写四元组或 区域.设置 登记过的名字。停止检测() 或脚本结束会停。",
                "example": (
                    '持续检测("yolo/xxx.onnx", 间隔=0.2)\n'
                    "当 真:\n"
                    "    循环 目标 在 检测.列表:\n"
                    '        如果 目标.类别 是 "怪物":\n'
                    "            点击(目标.x, 目标.y)\n"
                    "            中断\n"
                    "    延时(0.05)"
                ),
                "kind": "expr",
                "snippet": "持续检测(模型, 间隔=0.3)",
            },
            {
                "name": "停止检测",
                "signature": "停止检测()",
                "note": "停掉后台检测。脚本结束也会自动停。",
                "snippet": "停止检测()",
            },
            {
                "name": "持续找图",
                "signature": "持续找图(图片, 间隔=0.3)  /  持续找图(图片1, 图片2, 间隔=0.3)",
                "params": (("图片", "间隔=0.3"), ("图片", "图片2", "间隔=0.3")),
                "note": "后台反复找图，主脚本读 找图 就是最新一次。多图规则与找图相同。找到后不会自动点。",
                "kind": "expr",
                "snippet": "持续找图(图片, 间隔=0.3)",
            },
            {
                "name": "停止找图",
                "signature": "停止找图()",
                "note": "停掉后台找图。",
                "snippet": "停止找图()",
            },
            {
                "name": "多线程",
                "signature": "多线程(按W)",
                "params": (("按W",),),
                "note": (
                    "按W 是你自己起的子程序名，不是内置命令。先写 子程序 按W():，再 多线程(按W)，不要写成 多线程(按W())。每个脚本最多同时运行 32 个用户线程。"
                    "YOLO 不走多线程。一直认用 持续检测，结果在 检测 里，主流程和子程序都能读。"
                    "关一条写 关闭线程(按W)，全关写 关闭线程()。"
                ),
                "example": (
                    "子程序 按W():\n"
                    "    当 真:\n"
                    "        按键(\"W\", 秒=0.2)\n"
                    "        延时(0.05)\n"
                    "多线程(按W)\n"
                    '持续检测("yolo/xxx.onnx", 间隔=0.2)\n'
                    "当 真:\n"
                    "    循环 目标 在 检测.列表:\n"
                    '        如果 目标.类别 是 "怪物":\n'
                    "            点击(目标)\n"
                    "            中断\n"
                    "    延时(0.05)"
                ),
                "snippet": (
                    "子程序 按W():\n"
                    "    当 真:\n"
                    "        按键(\"W\", 秒=0.2)\n"
                    "        延时(0.05)\n"
                    "多线程(按W)"
                ),
            },
            {
                "name": "关闭线程",
                "signature": "关闭线程()  /  关闭线程(按W)",
                "params": ((), ("按W",)),
                "note": "关闭线程() 关掉本脚本开出的全部线程。关闭线程(按W) 只关这一条。按W 是你自己的子程序名，不要加括号。",
                "example": "关闭线程(按W)\n关闭线程()",
                "snippet": "关闭线程()",
            },
            {
                "name": "线程状态",
                "signature": "线程状态()  /  线程状态(按W)",
                "params": ((), ("按W",)),
                "note": "查询脚本线程状态。返回状态、是否运行、完成时间和异常；省略名字时返回全部线程。",
                "example": "状态 = 线程状态(按W)\n记录(状态.状态)",
                "snippet": "线程状态()",
            },
            {
                "name": "等待线程",
                "signature": "等待线程(按W, 超时=30)",
                "params": (("按W", "超时=30"),),
                "note": "等待指定线程结束。超时省略表示一直等待；返回值里可读取 状态、结果、错误。",
                "example": "结果 = 等待线程(按W, 超时=10)\n如果 结果.状态 是 \"failed\":\n    记录(结果.错误)",
                "snippet": "等待线程(按W, 超时=10)",
            },
            {
                "name": "线程结果",
                "signature": "线程结果(按W)",
                "params": (("按W",),),
                "note": "读取线程最近一次结果，不等待。线程尚未结束时状态为 running。",
                "example": "结果 = 线程结果(按W)\n记录(结果.结果)",
                "snippet": "线程结果(按W)",
            },
            {
                "name": "找所有图",
                "signature": "找所有图(图片, 阈值=0.8, 最多=20, 区域=(x, y, 宽, 高))  /  找所有图(图片1, 图片2, 阈值=0.8, 最多=20, 区域=(x, y, 宽, 高))",
                "params": (("图片", "阈值=0.8", "最多=20", "区域=(x, y, 宽, 高)"), ("图片", "图片2", "阈值=0.8", "最多=20", "区域=(x, y, 宽, 高)")),
                "note": "不做缩放、灰度和旋转。单图一次找出窗口里所有匹配。多张图与卡片多图一致：并行匹配，每张成功模板返回一个点。可写 图[0]、图.列表。",
                "kind": "expr",
                "snippet": "找所有图(图片, 阈值=0.8, 最多=20)",
            },
            {
                "name": "取色",
                "signature": "取色(x, y)",
                "params": (("x", "y"),),
                "note": "读当前窗口客户区这一点的颜色，返回 红,绿,蓝。可写 色.红。",
                "kind": "expr",
                "snippet": "取色(x, y)",
            },
            {
                "name": "比色",
                "signature": "比色(x, y, 颜色, 偏色=20)  /  比色(颜色, 偏色=20)",
                "params": (("x", "y", "颜色", "偏色=20"), ("颜色", "偏色=20")),
                "note": "和当前点或指定坐标比颜色。偏色是每个通道允许差多少。",
                "kind": "expr",
                "snippet": "比色(x, y, 颜色, 偏色=20)",
            },
            {
                "name": "按下",
                "signature": '按下(目标, 模式=无)  /  按下(x, y, 模式=无)  /  按下("W", 模式=无)',
                "params": (("目标", "模式=无"), ("x", "y", "模式=无"), ("按键", "模式=无")),
                "note": "只按下不松开。键盘写 按下(\"W\")，鼠标写 按下(目标) 或 按下(x, y)。和 松开 成对用。",
                "kind": "expr",
                "snippet": "按下(目标)",
            },
            {
                "name": "松开",
                "signature": '松开(目标, 模式=无)  /  松开(x, y, 模式=无)  /  松开("W", 模式=无)',
                "params": (("目标", "模式=无"), ("x", "y", "模式=无"), ("按键", "模式=无")),
                "note": "弹起。键盘写 松开(\"W\")，鼠标写 松开(目标)。",
                "kind": "expr",
                "snippet": "松开(目标)",
            },
            {
                "name": "按住",
                "signature": "按住(目标, 秒=0.5, 模式=无)  /  按住(\"W\", 秒=0.5, 模式=无)  /  按住(\"W\", 秒=(0.2, 0.6), 模式=无)",
                "params": (("目标", "秒=0.5", "模式=无"), ("按键", "秒=0.5", "模式=无"), ("按键", "秒=(0.2, 0.6)", "模式=无")),
                "note": "按下、等一段时间、再松开。秒写数字是固定时长，写 (最短, 最长) 或 随机(0.2, 0.6) 是随机时长。键盘鼠标都能用。",
                "kind": "expr",
                "snippet": "按住(目标, 秒=0.5)",
            },
            {
                "name": "连点",
                "signature": "连点(目标, 次数=3, 间隔=0.08, 模式=无)",
                "params": (("目标", "次数=3", "间隔=0.08", "模式=无"),),
                "note": "连续点击。间隔是每次之间的秒数。",
                "kind": "expr",
                "snippet": "连点(目标, 次数=3, 间隔=0.08)",
            },
            {
                "name": "等毫秒",
                "signature": "等毫秒(50)",
                "params": ("毫秒",),
                "note": "按毫秒等待。等毫秒(50) 就是等 50 毫秒。秒请用 延时(0.05)。",
                "snippet": "等毫秒(50)",
            },
            {
                "name": "鼠标位置",
                "signature": "鼠标位置()",
                "note": "当前鼠标在窗口客户区的坐标。",
                "kind": "expr",
                "snippet": "鼠标位置()",
            },
            {
                "name": "相对移动",
                "signature": "相对移动(偏移x, 偏移y)",
                "params": (("偏移x", "偏移y"),),
                "note": "从当前鼠标位置挪一段。正数向右、向下。",
                "snippet": "相对移动(偏移x, 偏移y)",
            },
            {
                "name": "播放",
                "signature": '播放(文件)  /  播放(文件, 等待=真)',
                "params": (("文件",), ("文件", "等待=真")),
                "note": "播音频。wav、mp3 都可以。等待=真 是播完再往下；等待=假 是接着跑。点停止会打断正在播的。文件用资源栏导入，或写成 \"提示.wav\"、\"sounds/提示.mp3\"。",
                "snippet": "播放(文件, 等待=真)",
            },
            {
                "name": "停止播放",
                "signature": "停止播放()",
                "note": "停掉当前正在播的音频。",
                "snippet": "停止播放()",
            },
            {
                "name": "回放",
                "signature": '回放("replays/过图.replay.json")  /  回放(文件, 速度=1, 次数=1, 等待=真)',
                "params": (("文件",), ("文件", "速度=1", "次数=1", "等待=真")),
                "note": "回放录制轨迹。文件用 .replay.json，放在 replays/ 或资源栏导入。等待=真 是播完再往下；等待=假 是后台播，可接着 暂停回放 / 继续回放 / 停止回放。同一时刻只能有一条回放，已经在播时再开会失败。只回放，不在脚本里开录制。",
                "snippet": '回放("replays/过图.replay.json", 速度=1, 次数=1, 等待=真)',
            },
            {
                "name": "暂停回放",
                "signature": "暂停回放()",
                "note": "暂停当前回放。没有回放时返回假，不会抛错。",
                "snippet": "暂停回放()",
            },
            {
                "name": "继续回放",
                "signature": "继续回放()",
                "note": "从暂停处继续当前回放。没有暂停的回放时返回假，不会抛错。",
                "snippet": "继续回放()",
            },
            {
                "name": "停止回放",
                "signature": "停止回放()",
                "note": "结束当前回放。没有回放时也算成功。",
                "snippet": "停止回放()",
            },
            {
                "name": "截图",
                "signature": "截图()  /  截图(\"shot.png\")",
                "params": ((), ("文件",)),
                "note": "把当前绑定窗口存成项目图片。不写文件名就自动起名。返回路径，可接着 找图(图.路径)。",
                "kind": "expr",
                "snippet": '截图("shot.png")',
            },
            {
                "name": "等按键",
                "signature": '等按键("F1", 超时=8, 间隔=0.05)  /  等按键("Ctrl+C", 超时=8, 间隔=0.05)',
                "params": (("按键", "超时=8", "间隔=0.05"),),
                "note": "等到这些键都按下。超时没按到就返回假，不会抛错。可写 空格、回车、F1 或组合键。",
                "kind": "expr",
                "snippet": '等按键("F1", 超时=8)',
            },
        ),
    },
    {
        "title": "窗口",
        "items": (
            {
                "name": "窗口.枚举",
                "signature": "窗口.枚举(标题=无, 类名=无, 可见=真)",
                "params": (("标题=无", "类名=无", "可见=真"),),
                "note": "列出可见顶级窗口，返回包含 hwnd、标题、类名、进程号和尺寸的字典列表。",
                "kind": "expr",
                "snippet": "窗口.枚举()",
            },
            {
                "name": "窗口.查找",
                "signature": "窗口.查找(标题, 完全匹配=假, 可见=真)",
                "params": (("标题", "完全匹配=假", "可见=真"),),
                "note": "按标题查找窗口，返回窗口信息列表；多个同名窗口不会被自动选中。",
                "kind": "expr",
                "snippet": '窗口.查找("记事本")',
            },
            {
                "name": "窗口.信息",
                "signature": "窗口.信息(目标=当前)",
                "params": (("目标=当前",),),
                "note": "读取当前绑定窗口或指定 HWND/标题的窗口信息。",
                "kind": "expr",
                "snippet": "窗口.信息()",
            },
            {
                "name": "窗口.激活",
                "signature": "窗口.激活(目标=当前)",
                "params": (("目标=当前",),),
                "note": "激活当前绑定窗口或指定窗口。",
                "snippet": "窗口.激活()",
            },
            {
                "name": "窗口.子窗口",
                "signature": "窗口.子窗口(目标=当前, 搜索深度=10, 可见=假)",
                "params": (("目标=当前", "搜索深度=10", "可见=假"),),
                "note": "按父子层级枚举目标窗口的子窗口，返回 parent_hwnd 和 depth。",
                "kind": "expr",
                "snippet": "窗口.子窗口()",
            },
            {
                "name": "窗口.消息",
                "signature": "窗口.消息(目标, 消息, wParam=0, lParam=0, 投递=真)",
                "params": (("目标", "消息", "wParam=0", "lParam=0", "投递=真"),),
                "note": "向窗口发送或投递受控消息；消息可写关闭、回车、复制、粘贴或整数消息号。",
                "kind": "expr",
                "snippet": "窗口.消息(1, 0, 投递=真)",
            },
            {
                "name": "窗口.Z序",
                "signature": "窗口.Z序(可见=真)",
                "params": (("可见=真",),),
                "note": "按系统 Z 序返回顶级窗口列表，每项含 z_index。",
                "kind": "expr",
                "snippet": "窗口.Z序()",
            },
            {
                "name": "窗口.DPI",
                "signature": "窗口.DPI(目标=当前)",
                "params": (("目标=当前",),),
                "note": "读取窗口 DPI 和缩放比例，返回 结果.dpi、结果.scale。",
                "kind": "expr",
                "snippet": "窗口.DPI()",
            },
            {
                "name": "窗口.坐标转换",
                "signature": "窗口.坐标转换(x, y, 方向=\"屏幕到客户区\", 目标=当前)",
                "params": (("x", "y", "方向=屏幕到客户区", "目标=当前"),),
                "note": "在屏幕坐标和目标窗口客户区坐标之间转换，支持多显示器负坐标。",
                "kind": "expr",
                "snippet": "点 = 窗口.坐标转换(100, 200, 方向=\"屏幕到客户区\")",
            },
            {
                "name": "窗口.兼容性",
                "signature": "窗口.兼容性(目标=当前)",
                "params": (("目标=当前",),),
                "note": "诊断 DPI 感知、截图引擎可用性及失败原因、DirectX/OpenGL 能力、当前进程与目标窗口管理员权限、目标窗口 DPI 和线程/截图并发资源，供脚本选择合适的识别/输入策略。",
                "kind": "expr",
                "snippet": "环境 = 窗口.兼容性()\n记录(环境.截图)",
            },
            {
                "name": "窗口.查找元素",
                "signature": "窗口.查找元素(名称=无, 自动化ID=无, 类名=无, 控件类型=无, 序号=0, 搜索深度=30, 超时=5, 目标=当前)",
                "params": (("名称=无", "自动化ID=无", "类名=无", "控件类型=无", "序号=0", "搜索深度=30", "超时=5", "目标=当前"),),
                "note": "按 UI Automation 属性查找元素，返回元素信息列表；结果可读 列表、名称、自动化ID、类名、控件类型、位置和尺寸。",
                "kind": "expr",
                "snippet": "窗口.查找元素(名称=\"确定\")",
            },
            {
                "name": "窗口.元素文本",
                "signature": "窗口.元素文本(名称=无, 自动化ID=无, 类名=无, 控件类型=无, 序号=0, 搜索深度=30, 超时=5, 目标=当前)",
                "params": (("名称=无", "自动化ID=无", "类名=无", "控件类型=无", "序号=0", "搜索深度=30", "超时=5", "目标=当前"),),
                "note": "读取 UI Automation 元素的 ValuePattern、TextPattern 或 Name 文本。",
                "kind": "expr",
                "snippet": "窗口.元素文本(名称=\"状态\")",
            },
            {
                "name": "窗口.设置元素值",
                "signature": "窗口.设置元素值(值, 名称=无, 自动化ID=无, 类名=无, 控件类型=无, 序号=0, 搜索深度=30, 超时=5, 目标=当前)",
                "params": (("值", "名称=无", "自动化ID=无", "类名=无", "控件类型=无", "序号=0", "搜索深度=30", "超时=5", "目标=当前"),),
                "note": "设置 UI Automation 编辑控件的值，优先使用 ValuePattern，失败时使用聚焦后输入。",
                "snippet": "窗口.设置元素值(\"内容\", 名称=\"输入框\")",
            },
            {
                "name": "窗口.元素属性",
                "signature": "窗口.元素属性(名称=无, 自动化ID=无, 类名=无, 控件类型=无, 序号=0, 搜索深度=30, 超时=5, 目标=当前)",
                "params": (("名称=无", "自动化ID=无", "类名=无", "控件类型=无", "序号=0", "搜索深度=30", "超时=5", "目标=当前"),),
                "note": "读取 UI Automation 元素属性、可用状态、焦点状态和屏幕矩形。",
                "kind": "expr",
                "snippet": "窗口.元素属性(名称=\"确定\")",
            },
            {
                "name": "窗口.元素聚焦",
                "signature": "窗口.元素聚焦(名称=无, 自动化ID=无, 类名=无, 控件类型=无, 序号=0, 搜索深度=30, 超时=5, 目标=当前)",
                "params": (("名称=无", "自动化ID=无", "类名=无", "控件类型=无", "序号=0", "搜索深度=30", "超时=5", "目标=当前"),),
                "note": "将键盘焦点设置到目标元素。",
                "kind": "expr",
                "snippet": "窗口.元素聚焦(名称=\"输入框\")",
            },
            {
                "name": "窗口.元素切换",
                "signature": "窗口.元素切换(名称=无, 自动化ID=无, 类名=无, 控件类型=无, 序号=0, 状态=无, 目标=当前)",
                "params": (("名称=无", "自动化ID=无", "类名=无", "控件类型=无", "序号=0", "状态=无", "目标=当前"),),
                "note": "切换复选框或其他 Toggle 控件；状态省略时反转，指定真/假时设置目标状态。",
                "kind": "expr",
                "snippet": "窗口.元素切换(名称=\"记住我\", 状态=真)",
            },
            {
                "name": "窗口.元素选择",
                "signature": "窗口.元素选择(值=无, 名称=无, 自动化ID=无, 类名=无, 控件类型=无, 序号=0, 目标=当前)",
                "params": (("值=无", "名称=无", "自动化ID=无", "类名=无", "控件类型=无", "序号=0", "目标=当前"),),
                "note": "选择下拉框、列表或树节点；传入值时优先按名称定位列表项。",
                "kind": "expr",
                "snippet": "窗口.元素选择(值=\"选项一\", 控件类型=\"下拉框\")",
            },
            {
                "name": "窗口.元素展开 / 窗口.元素折叠",
                "signature": "窗口.元素展开(名称=无, ...)  /  窗口.元素折叠(名称=无, ...)",
                "params": (("名称=无", "自动化ID=无", "类名=无", "控件类型=无", "序号=0", "目标=当前"),),
                "note": "展开或折叠支持 ExpandCollapsePattern 的树节点、菜单或面板。",
                "kind": "expr",
                "snippet": "窗口.元素展开(名称=\"项目\")",
            },
            {
                "name": "窗口.设置分辨率",
                "signature": (
                    "窗口.设置分辨率(宽, 高)\n"
                    "窗口.设置分辨率(宽, 高, 目标)\n"
                    "窗口.设置分辨率(目标)\n"
                    "窗口.设置分辨率(宽, 高, 报错=假)"
                ),
                "params": (
                    ("宽", "高"),
                    ("宽", "高", "目标"),
                    ("目标",),
                    ("宽", "高", "报错=假"),
                ),
                "note": "调整窗口客户区分辨率。窗口相关能力统一放在本分类。",
                "snippet": "窗口.设置分辨率(1280, 720)",
            },
            {
                "name": "窗口.关闭",
                "signature": "窗口.关闭(目标=当前)",
                "params": (("目标=当前",),),
                "note": "向窗口发送 WM_CLOSE，给目标程序自行清理的机会，不强制结束进程。",
                "snippet": "窗口.关闭()",
            },
            {
                "name": "窗口.显示 / 窗口.隐藏",
                "signature": "窗口.显示(目标=当前)  /  窗口.隐藏(目标=当前)",
                "params": (("目标=当前",),),
                "note": "显示或隐藏指定窗口。",
                "snippet": "窗口.显示()",
            },
            {
                "name": "窗口.最小化 / 窗口.还原",
                "signature": "窗口.最小化(目标=当前)  /  窗口.还原(目标=当前)",
                "params": (("目标=当前",),),
                "note": "最小化或还原指定窗口。",
                "snippet": "窗口.还原()",
            },
            {
                "name": "窗口.移动 / 窗口.大小",
                "signature": "窗口.移动(x, y, 目标=当前)  /  窗口.大小(宽, 高, 目标=当前)",
                "params": (("x", "y", "目标=当前"), ("宽", "高", "目标=当前")),
                "note": "调整窗口外框位置或尺寸，坐标和尺寸使用屏幕像素。",
                "snippet": "窗口.移动(100, 100)",
            },
            {
                "name": "窗口.宽 / 窗口.高",
                "signature": "窗口.宽  /  窗口.高",
                "note": "当前绑定窗口客户区尺寸。窗口信息也可以从 窗口.信息() 读取。",
                "kind": "expr",
                "snippet": "窗口.宽",
            },
        ),
    },
    {
        "title": "进程",
        "items": (
            {
                "name": "进程.查找 / 进程.信息 / 进程.是否运行",
                "signature": "进程.查找(名称=无)  /  进程.信息(进程号)  /  进程.是否运行(目标)",
                "params": (("名称=无",), ("进程号",), ("目标",)),
                "note": "读取本机进程列表或状态。目标可以是进程号或名称。",
                "kind": "expr",
                "snippet": "进程.查找()",
            },
            {
                "name": "进程.启动 / 进程.关闭",
                "signature": "进程.启动(程序, 参数=无, 等待=假)  /  进程.关闭(进程号, 强制=假)",
                "params": (("程序", "参数=无", "等待=假"), ("进程号", "强制=假")),
                "note": "启动和结束进程属于外部操作，必须启用并信任当前脚本的外部组件权限；启动不经过命令行 Shell。",
                "kind": "expr",
                "snippet": '进程.启动("notepad.exe")',
            },
        ),
    },
    {
        "title": "性能",
        "items": (
            {
                "name": "性能.快照",
                "signature": "性能.快照()",
                "params": ((),),
                "note": "读取当前进程内存、CPU、线程数及截图工作线程统计；缺少 psutil 时返回不可用。",
                "kind": "expr",
                "snippet": "性能状态 = 性能.快照()\n记录(性能状态.rss_mb)",
            },
        ),
    },
    {
        "title": "热键与定时器",
        "items": (
            {
                "name": "热键.注册",
                "signature": "热键.注册(按键, 回调, 名字=无)",
                "params": (("按键", "回调", "名字=无"),),
                "note": "注册当前脚本拥有的全局热键。回调必须是脚本子程序；脚本结束或停止时自动注销。",
                "kind": "expr",
                "snippet": '热键.注册("F6", 回调)',
            },
            {
                "name": "热键.注销 / 热键.全部注销",
                "signature": "热键.注销(名字)  /  热键.全部注销()",
                "params": (("名字",), ()),
                "note": "注销指定名称的热键，或注销当前脚本注册的全部热键。",
                "snippet": '热键.注销("F6")',
            },
            {
                "name": "定时器.启动",
                "signature": "定时器.启动(名字, 间隔, 回调, 次数=0)",
                "params": (("名字", "间隔", "回调", "次数=0"),),
                "note": "后台按间隔调用脚本子程序；次数为 0 表示持续运行。",
                "kind": "expr",
                "snippet": '定时器.启动("心跳", 1, 心跳)',
            },
            {
                "name": "定时器.停止",
                "signature": "定时器.停止(名字)",
                "params": (("名字",),),
                "note": "停止指定名称的脚本定时器。",
                "snippet": '定时器.停止("心跳")',
            },
        ),
    },
    {
        "title": "输入设备",
        "items": (
            {
                "name": "输入设备.枚举",
                "signature": "输入设备.枚举()",
                "note": "枚举 WinMM 游戏杆；无设备时返回空列表。",
                "kind": "expr",
                "snippet": "设备列表 = 输入设备.枚举()",
            },
            {
                "name": "输入设备.能力",
                "signature": "输入设备.能力()",
                "note": "报告本机可用的输入设备协议（WinMM、hidapi、虚拟手柄）；只探测不注入。",
                "kind": "expr",
                "snippet": "能力 = 输入设备.能力()\n记录(能力.hidapi)",
            },
            {
                "name": "输入设备.状态",
                "signature": "输入设备.状态(设备=0)",
                "params": (("设备=0",),),
                "note": "读取游戏杆轴、按钮位图和方向帽状态；不模拟输入。",
                "kind": "expr",
                "snippet": "状态 = 输入设备.状态(0)\n记录(状态.摇杆X)",
            },
            {
                "name": "输入设备.等待按钮",
                "signature": "输入设备.等待按钮(按钮, 设备=0, 按下=真, 超时=30, 间隔=0.05)",
                "params": (("按钮", "设备=0", "按下=真", "超时=30", "间隔=0.05"),),
                "note": "等待游戏杆按钮达到指定状态；支持超时和脚本停止检查。",
                "kind": "expr",
                "snippet": "输入设备.等待按钮(1, 超时=10)",
            },
            {
                "name": "输入设备.HID枚举",
                "signature": "输入设备.HID枚举()",
                "note": "通过可选 hidapi 枚举 HID 设备；库或驱动不可用时返回明确错误。",
                "kind": "expr",
                "snippet": "HID = 输入设备.HID枚举()\n记录(HID.设备)",
            },
            {
                "name": "输入设备.HID读取",
                "signature": "输入设备.HID读取(路径, 长度=64, 超时=1000)",
                "params": (("路径", "长度=64", "超时=1000"),),
                "note": "从指定 HID 路径读取报告，限制报告长度和超时；不会写入设备。",
                "kind": "expr",
                "snippet": "报告 = 输入设备.HID读取(路径, 长度=64, 超时=1000)",
            },
            {
                "name": "输入设备.HID写入",
                "signature": "输入设备.HID写入(路径, 数据, 超时=1000)",
                "params": (("路径", "数据", "超时=1000"),),
                "note": "向 HID 设备写入报告；必须启用并信任外部组件，数据限制为 1～4096 字节。",
                "kind": "expr",
                "snippet": "输入设备.HID写入(路径, [0, 1, 2])",
            },
        ),
    },
    {
        "title": "跳转",
        "items": (
            {
                "name": "记录",
                "signature": "记录(内容)",
                "params": ("内容",),
                "note": "写到运行日志，不影响成功失败。",
                "snippet": "记录(内容)",
            },
            {
                "name": "成功",
                "signature": "成功()  /  成功(说明)",
                "params": ((), ("说明",)),
                "note": "立刻结束脚本并走成功线。后面的命令不会执行。",
                "snippet": "成功()",
            },
            {
                "name": "失败",
                "signature": "失败(说明)",
                "params": ("说明",),
                "note": "立刻结束脚本并走失败线。后面的命令不会执行。",
                "snippet": "失败(说明)",
            },
        ),
    },
    {
        "title": "外部组件",
        "items": (
            {
                "name": "组件.加载",
                "signature": '组件.加载(类型, 入口, 调用约定="cdecl", 超时=10)',
                "params": (("类型", "入口"), ("类型", "入口", '调用约定="cdecl"', "超时=10")),
                "note": "加载进程、Python、COM 或 DLL 组件，返回组件句柄。本卡必须启用“允许外部组件”。DLL 的错误签名只会终止隔离宿主。",
                "kind": "expr",
                "snippet": '组件.加载("dll", "plugins/example.dll", 调用约定="cdecl")',
            },
            {
                "name": "组件.调用",
                "signature": "组件.调用(句柄, 方法, *参数, 参数类型=空, 返回类型=\"int32\", 关键字参数=空, 超时=30, 输入=空, 工作目录=空, 环境变量=空, 编码=\"utf-8\")",
                "params": (
                    ("句柄", "方法"),
                    ("句柄", "方法", "参数"),
                    ("句柄", "方法", "参数", "参数类型=空", '返回类型="int32"', "关键字参数=空", "超时=30", "输入=空", "工作目录=空", "环境变量=空", '编码="utf-8"'),
                ),
                "note": "统一调用 Python/COM 方法、DLL 导出函数或已加载的进程。DLL 必须声明每个参数类型；Python/COM 关键字参数写成字典。",
                "kind": "expr",
                "snippet": '组件.调用(插件, "方法名", 参数)',
            },
            {
                "name": "组件.读取",
                "signature": "组件.读取(句柄, 属性, 超时=30)",
                "params": (("句柄", "属性"), ("句柄", "属性", "超时=30")),
                "note": "读取 Python 或 COM 对象属性。支持用点分隔的嵌套属性路径。",
                "kind": "expr",
                "snippet": '组件.读取(插件, "Version")',
            },
            {
                "name": "组件.写入",
                "signature": "组件.写入(句柄, 属性, 值, 超时=30)",
                "params": (("句柄", "属性", "值"), ("句柄", "属性", "值", "超时=30")),
                "note": "写入 Python 或 COM 对象属性。",
                "snippet": '组件.写入(插件, "Enabled", 真)',
            },
            {
                "name": "组件.运行",
                "signature": "组件.运行(程序, *参数, 超时=30, 输入=空, 工作目录=空, 环境变量=空, 编码=\"utf-8\")",
                "params": (("程序",), ("程序", "参数", "超时=30", "输入=空", "工作目录=空", "环境变量=空", '编码="utf-8"')),
                "note": "运行外部程序并等待退出。返回退出码、标准输出、标准错误和进程号；不经过命令行外壳。",
                "kind": "expr",
                "snippet": '组件.运行("tools/helper.exe", "--version", 超时=30)',
            },
            {
                "name": "组件.关闭",
                "signature": "组件.关闭(句柄, 超时=5)  /  组件.关闭()",
                "params": (("句柄", "超时=5"), ()),
                "note": "释放一个组件；不传句柄则释放本脚本打开的全部组件。脚本结束也会自动释放。",
                "snippet": "组件.关闭(插件)",
            },
        ),
    },
    {
        "title": "变量",
        "items": (
            {
                "name": "变量.获取",
                "signature": "变量.获取(名字, 默认值)",
                "params": ("名字", "默认值"),
                "note": "没有这个名字时返回后面的默认值。只在本张脚本卡。",
                "kind": "expr",
                "snippet": "变量.获取(名字, 默认值)",
            },
            {
                "name": "变量.设置",
                "signature": "变量.设置(名字, 值)",
                "params": ("名字", "值"),
                "note": "写入或覆盖。只在本张脚本卡。",
                "snippet": "变量.设置(名字, 值)",
            },
            {
                "name": "变量.增加",
                "signature": "变量.增加(名字, 步长=1)",
                "params": ("名字", "步长=1"),
                "note": "按步长累加，默认加 1。只在本张脚本卡。",
                "snippet": "变量.增加(名字)",
            },
            {
                "name": "变量.存在",
                "signature": "变量.存在(名字)",
                "params": ("名字",),
                "note": "判断变量是否已定义。只在本张脚本卡。",
                "kind": "expr",
                "snippet": "变量.存在(名字)",
            },
            {
                "name": "变量.删除",
                "signature": "变量.删除(名字)",
                "params": ("名字",),
                "note": "删除变量。只在本张脚本卡。",
                "kind": "expr",
                "snippet": "变量.删除(名字)",
            },
            {
                "name": "变量.全局获取",
                "signature": "变量.全局获取(名字, 默认值)",
                "params": ("名字", "默认值"),
                "note": "没有这个名字时返回后面的默认值。同一次工作流跑程内所有脚本卡共享。",
                "kind": "expr",
                "snippet": "变量.全局获取(名字, 默认值)",
            },
            {
                "name": "变量.全局设置",
                "signature": "变量.全局设置(名字, 值)",
                "params": ("名字", "值"),
                "note": "写入或覆盖。同一次工作流跑程内所有脚本卡共享。卡之间传值请用此命令。",
                "snippet": "变量.全局设置(名字, 值)",
            },
            {
                "name": "变量.全局增加",
                "signature": "变量.全局增加(名字, 步长=1)",
                "params": ("名字", "步长=1"),
                "note": "按步长累加，默认加 1。同一次工作流跑程内所有脚本卡共享。",
                "snippet": "变量.全局增加(名字)",
            },
            {
                "name": "变量.全局存在",
                "signature": "变量.全局存在(名字)",
                "params": ("名字",),
                "note": "判断全局变量是否已定义。同一次工作流跑程内所有脚本卡共享。",
                "kind": "expr",
                "snippet": "变量.全局存在(名字)",
            },
            {
                "name": "变量.全局删除",
                "signature": "变量.全局删除(名字)",
                "params": ("名字",),
                "note": "删除全局变量。同一次工作流跑程内所有脚本卡共享。",
                "kind": "expr",
                "snippet": "变量.全局删除(名字)",
            },
            {
                "name": "剪贴板.获取",
                "signature": "剪贴板.获取()",
                "note": "读当前剪贴板文字。",
                "kind": "expr",
                "snippet": "剪贴板.获取()",
            },
            {
                "name": "剪贴板.设置",
                "signature": "剪贴板.设置(文本)",
                "params": ("文本",),
                "note": "写入剪贴板。",
                "snippet": "剪贴板.设置(文本)",
            },
            {
                "name": "数据.读取",
                "signature": "数据.读取(文件名, 默认值=\"\")",
                "params": (("文件名", "默认值=\"\""),),
                "note": "读取脚本专属数据目录中的文本文件，只允许相对路径。",
                "kind": "expr",
                "snippet": "数据.读取(文件名)",
            },
            {
                "name": "数据.写入",
                "signature": "数据.写入(文件名, 内容)",
                "params": (("文件名", "内容"),),
                "note": "写入脚本专属数据目录，单文件有大小限制。",
                "kind": "expr",
                "snippet": "数据.写入(文件名, 内容)",
            },
            {
                "name": "数据.JSON读取",
                "signature": "数据.JSON读取(文件名, 默认值=None)",
                "params": (("文件名", "默认值=None"),),
                "note": "读取并解析脚本专属 JSON 数据。",
                "kind": "expr",
                "snippet": "数据.JSON读取(文件名)",
            },
            {
                "name": "数据.JSON写入",
                "signature": "数据.JSON写入(文件名, 对象)",
                "params": (("文件名", "对象"),),
                "note": "将对象保存为脚本专属 JSON 数据。",
                "kind": "expr",
                "snippet": "数据.JSON写入(文件名, 对象)",
            },
            {
                "name": "数据.INI读取",
                "signature": "数据.INI读取(文件名, 节, 键, 默认值=\"\")",
                "params": (("文件名", "节", "键", "默认值=\"\""),),
                "note": "读取脚本专属数据目录中的 INI 配置值。",
                "kind": "expr",
                "snippet": "数据.INI读取(\"config.ini\", \"设置\", \"主题\")",
            },
            {
                "name": "数据.INI写入",
                "signature": "数据.INI写入(文件名, 节, 键, 值)",
                "params": (("文件名", "节", "键", "值"),),
                "note": "写入脚本专属数据目录中的 INI 配置值。",
                "kind": "expr",
                "snippet": "数据.INI写入(\"config.ini\", \"设置\", \"主题\", \"暗色\")",
            },
            {
                "name": "数据.CSV读取",
                "signature": "数据.CSV读取(文件名, 标题行=真, 最多=10000)",
                "params": (("文件名", "标题行=真", "最多=10000"),),
                "note": "读取受限大小的 CSV；有标题行时返回字典列表，否则返回二维列表。",
                "kind": "expr",
                "snippet": "行 = 数据.CSV读取(\"items.csv\")",
            },
            {
                "name": "数据.SQLite查询",
                "signature": "数据.SQLite查询(文件名, SQL, 参数=(), 最多=10000)",
                "params": (("文件名", "SQL", "参数=()", "最多=10000"),),
                "note": "以只读方式执行 SQLite SELECT/PRAGMA/WITH 查询，并限制结果行数。",
                "kind": "expr",
                "snippet": "行 = 数据.SQLite查询(\"data.db\", \"SELECT * FROM items WHERE id=?\", (1,))",
            },
            {
                "name": "数据.存在",
                "signature": "数据.存在(文件名)",
                "params": (("文件名",),),
                "note": "判断脚本数据文件是否存在。",
                "kind": "expr",
                "snippet": "数据.存在(文件名)",
            },
            {
                "name": "数据.删除",
                "signature": "数据.删除(文件名)",
                "params": (("文件名",),),
                "note": "删除脚本专属数据文件。",
                "kind": "expr",
                "snippet": "数据.删除(文件名)",
            },
        ),
    },
    {
        "title": "区域",
        "items": (
            {
                "name": "区域.设置",
                "signature": '区域.设置("打怪", 80, 120, 640, 360)  /  区域.设置("打怪", (80, 120, 640, 360))',
                "params": (('"打怪"', "80", "120", "640", "360"), ('"打怪"', "(80, 120, 640, 360)")),
                "note": "登记本张脚本卡的识别区。用工具栏「框选区域」写入；这一次执行有效，结束即丢。宽、高必须大于 0。",
                "snippet": '区域.设置("区域1", 80, 120, 640, 360)',
            },
            {
                "name": "区域.获取",
                "signature": '区域.获取("打怪")',
                "params": (('"打怪"',),),
                "note": "返回 区.x 区.y 区.宽 区.高；没有这个名字时 通过=假。",
                "kind": "expr",
                "snippet": '区域.获取("区域1")',
            },
            {
                "name": "区域.存在",
                "signature": '区域.存在("打怪")',
                "params": (('"打怪"',),),
                "note": "本卡是否已登记这个区域。",
                "kind": "expr",
                "snippet": '区域.存在("区域1")',
            },
            {
                "name": "区域.删除",
                "signature": '区域.删除("打怪")',
                "params": (('"打怪"',),),
                "note": "删除本卡已登记区域。",
                "kind": "expr",
                "snippet": '区域.删除("区域1")',
            },
            {
                "name": "区域.列表",
                "signature": "区域.列表()",
                "note": "列出本卡已登记区域。",
                "kind": "expr",
                "snippet": "区域.列表()",
            },
        ),
    },
    {
        "title": "网络",
        "items": (
            {
                "name": "网络.请求",
                "signature": "网络.请求(地址, 方法=\"GET\", 数据=无, 头=无, 超时=15, 最大字节=2097152)",
                "params": (("地址", "方法=\"GET\"", "数据=无", "头=无", "超时=15", "最大字节=2097152"),),
                "note": "执行受限 HTTP 请求，仅允许 http/https，响应大小最多 10 MB；需要启用并信任外部组件。",
                "kind": "expr",
                "snippet": "网络.请求(\"https://example.com\")",
            },
        ),
    },
    {
        "title": "事件",
        "items": (
            {
                "name": "事件.等待窗口",
                "signature": "事件.等待窗口(标题, 完全匹配=假, 超时=30, 间隔=0.3)",
                "params": (("标题", "完全匹配=假", "超时=30", "间隔=0.3"),),
                "note": "等待窗口出现；超时返回假，可被停止请求中断。",
                "kind": "expr",
                "snippet": "事件.等待窗口(\"记事本\", 超时=10)",
            },
            {
                "name": "事件.等待进程",
                "signature": "事件.等待进程(目标, 超时=30, 间隔=0.5)",
                "params": (("目标", "超时=30", "间隔=0.5"),),
                "note": "等待指定 PID 或进程名运行；超时返回假。",
                "kind": "expr",
                "snippet": "事件.等待进程(\"notepad.exe\", 超时=10)",
            },
            {
                "name": "事件.等待窗口关闭",
                "signature": "事件.等待窗口关闭(目标, 超时=30, 间隔=0.3)",
                "params": (("目标", "超时=30", "间隔=0.3"),),
                "note": "等待 HWND 或窗口标题对应窗口关闭；超时返回假，可被停止请求中断。",
                "kind": "expr",
                "snippet": "事件.等待窗口关闭(窗口, 超时=10)",
            },
            {
                "name": "事件.等待进程退出",
                "signature": "事件.等待进程退出(目标, 超时=30, 间隔=0.5)",
                "params": (("目标", "超时=30", "间隔=0.5"),),
                "note": "等待指定 PID 或进程名退出；超时返回假。",
                "kind": "expr",
                "snippet": "事件.等待进程退出(1234, 超时=10)",
            },
            {
                "name": "事件.等待文件",
                "signature": "事件.等待文件(文件, 存在=真, 超时=30, 间隔=0.5)",
                "params": (("文件", "存在=真", "超时=30", "间隔=0.5"),),
                "note": "等待绝对路径文件出现或消失；超时返回假。",
                "kind": "expr",
                "snippet": "事件.等待文件(\"C:/temp/完成.flag\", 超时=10)",
            },
        ),
    },
    {
        "title": "结果",
        "items": (
            {
                "name": "上次",
                "signature": "上次.通过  上次.成功  上次.分数  上次.阈值  上次.x  上次.y",
                "note": "最近一次动作的结果，不区分种类。",
                "kind": "expr",
                "snippet": "上次",
            },
            {
                "name": "文字",
                "signature": "文字.内容  文字.通过",
                "note": "最近一次找字或字库识别。",
                "kind": "expr",
                "snippet": "文字",
            },
            {
                "name": "卡片",
                "signature": "卡片[编号].内容  卡片[编号].分数  卡片[编号].通过",
                "note": "读画布上某张卡上次跑完的结果，编号是卡片编号。",
                "kind": "expr",
                "snippet": "卡片[编号]",
            },
        ),
    },
    {
        "title": "语法",
        "items": (
            {
                "name": "如果",
                "signature": "如果 条件:",
                "params": ("条件",),
                "note": "条件成立时执行缩进里的命令。回车后在下一行写动作。",
                "snippet": "如果 条件:",
            },
            {
                "name": "循环",
                "signature": "循环 i 在 范围(次数):",
                "params": ("次数",),
                "note": "按次数重复。写成 循环 i 在 范围(6): 或 循环 i 在 6:。循环里不要写 成功()/失败()，那会提前结束整段脚本。",
                "snippet": "循环 i 在 范围(次数):",
            },
            {
                "name": "当",
                "signature": "当 条件:",
                "params": ("条件",),
                "note": "条件成立就继续。",
                "snippet": "当 条件:",
            },
            {
                "name": "否则如果",
                "signature": "否则如果 条件:",
                "params": ("条件",),
                "note": "上一个条件不成立时再判断。",
                "snippet": "否则如果 条件:",
            },
            {
                "name": "否则",
                "signature": "否则:",
                "note": "前面的条件都不成立时执行。与 如果 对齐。",
                "snippet": "否则:",
            },
            {
                "name": "中断",
                "signature": "中断",
                "note": "立刻跳出当前循环。",
                "snippet": "中断",
            },
            {
                "name": "继续",
                "signature": "继续",
                "note": "跳过本轮，进入下一轮循环。",
                "snippet": "继续",
            },
            {
                "name": "子程序",
                "signature": "子程序 点确定():",
                "note": "可复用的一段命令。里面可以用 返回 值。可以再写子程序。",
                "snippet": "子程序 点确定():",
            },
            {"name": "函数", "signature": "函数 点确定():", "note": "子程序的同义写法。", "snippet": "函数 点确定():"},
            {"name": "返回", "signature": "返回 值", "params": ("值",), "note": "从当前子程序返回结果。", "snippet": "返回 值"},
            {"name": "略过", "signature": "略过", "note": "空操作，占位或表示暂不执行。", "snippet": "略过"},
        ),
    },
    {
        "title": "计算",
        "items": (
            {
                "name": "长度",
                "signature": "长度(对象)",
                "params": ("对象",),
                "note": "文字或列表有多少项。",
                "kind": "expr",
                "snippet": "长度(对象)",
            },
            {
                "name": "整数",
                "signature": "整数(值)",
                "params": ("值",),
                "note": "转成整数。",
                "kind": "expr",
                "snippet": "整数(值)",
            },
            {
                "name": "小数",
                "signature": "小数(值)",
                "params": ("值",),
                "note": "转成小数。",
                "kind": "expr",
                "snippet": "小数(值)",
            },
            {
                "name": "到文本",
                "signature": "到文本(值)",
                "params": ("值",),
                "note": "转成文字。",
                "kind": "expr",
                "snippet": "到文本(值)",
            },
            {
                "name": "真假",
                "signature": "真假(值)",
                "params": ("值",),
                "note": "转成真或假。",
                "kind": "expr",
                "snippet": "真假(值)",
            },
            {
                "name": "最小",
                "signature": "最小(值1, 值2)",
                "params": ("值1", "值2"),
                "note": "取较小的那个。",
                "kind": "expr",
                "snippet": "最小(值1, 值2)",
            },
            {
                "name": "最大",
                "signature": "最大(值1, 值2)",
                "params": ("值1", "值2"),
                "note": "取较大的那个。",
                "kind": "expr",
                "snippet": "最大(值1, 值2)",
            },
            {
                "name": "绝对值",
                "signature": "绝对值(值)",
                "params": ("值",),
                "note": "去掉正负号。",
                "kind": "expr",
                "snippet": "绝对值(值)",
            },
            {
                "name": "开方",
                "signature": "开方(值)  /  平方根(值)",
                "params": ("值",),
                "note": "平方根，不是「开放」。开方(9) 是 3。算距离用 开方(差x * 差x + 差y * 差y)。",
                "kind": "expr",
                "snippet": "开方(值)",
            },
            {"name": "平方根", "signature": "平方根(值)", "params": ("值",), "note": "开方的同义写法。", "kind": "expr", "snippet": "平方根(值)"},
            {
                "name": "限制",
                "signature": "限制(值, 下限, 上限)",
                "params": ("值", "下限", "上限"),
                "note": "把数夹在两头之间，防止点出窗口或区域。",
                "kind": "expr",
                "snippet": "限制(值, 下限, 上限)",
            },
            {
                "name": "距离",
                "signature": "距离(点1, 点2)  /  距离(x1, y1, x2, y2)",
                "params": (("点1", "点2"), ("x1", "y1", "x2", "y2")),
                "note": "用两点的 x、y 算像素距离。也可以自己用 开方 算。",
                "kind": "expr",
                "snippet": "距离(点1, 点2)",
            },
            {
                "name": "角度",
                "signature": "角度(点1, 点2)  /  角度(x1, y1, x2, y2)",
                "params": (("点1", "点2"), ("x1", "y1", "x2", "y2")),
                "note": "从点1指向点2的方向角，单位是度。用两点的 x、y。屏幕坐标：0 正右，90 正下，-90 正上，180 正左。",
                "kind": "expr",
                "snippet": "角度(点1, 点2)",
            },
            {
                "name": "正弦",
                "signature": "正弦(角度)",
                "params": ("角度",),
                "note": "参数是度，不是弧度。和 余弦 一起把角度还原成方向。",
                "kind": "expr",
                "snippet": "正弦(角度)",
            },
            {
                "name": "余弦",
                "signature": "余弦(角度)",
                "params": ("角度",),
                "note": "参数是度，不是弧度。走x = 自己.x + 余弦(角) * 像素。",
                "kind": "expr",
                "snippet": "余弦(角度)",
            },
            {
                "name": "范围",
                "signature": "范围(次数)  /  范围(起点, 终点)  /  范围(起点, 终点, 步长)",
                "params": (("次数",), ("起点", "终点"), ("起点", "终点", "步长")),
                "note": "给循环生成一串数字。",
                "kind": "expr",
                "snippet": "范围(次数)",
            },
            {
                "name": "随机",
                "signature": "随机()  /  随机(10)  /  随机(1, 10)",
                "params": ((), ("次数",), ("起点", "终点")),
                "note": "不写参数是 0 到 1 的小数。写一个整数是 0 到 这个数减 1。写两个整数是闭区间整数。写两个小数是这段里随机小数，给按住时长用。",
                "kind": "expr",
                "snippet": "随机(1, 10)",
            },
            {
                "name": "包含",
                "signature": "包含(文本, 片段)",
                "params": ("文本", "片段"),
                "note": "文本里有没有这段字。",
                "kind": "expr",
                "snippet": "包含(文本, 片段)",
            },
            {
                "name": "截取",
                "signature": "截取(文本, 起点, 字数)",
                "params": ("文本", "起点", "字数"),
                "note": "起点从 1 开始。不写字数就截到末尾。",
                "kind": "expr",
                "snippet": "截取(文本, 1, 2)",
            },
            {
                "name": "替换",
                "signature": "替换(文本, 旧, 新)",
                "params": ("文本", "旧", "新"),
                "note": "把旧字换成新字。",
                "kind": "expr",
                "snippet": "替换(文本, 旧, 新)",
            },
            {
                "name": "分割",
                "signature": '分割(文本, ",")',
                "params": ("文本", "分隔符"),
                "note": "拆成列表。不写分隔符就按空白拆。",
                "kind": "expr",
                "snippet": "分割(文本, 分隔符)",
            },
            {
                "name": "时间",
                "signature": "时间()",
                "note": "当前毫秒。用来自己算超时：起 = 时间()。",
                "kind": "expr",
                "snippet": "时间()",
            },
            {
                "name": "去空格",
                "signature": "去空格(文本)",
                "params": ("文本",),
                "note": "去掉头尾空白。",
                "kind": "expr",
                "snippet": "去空格(文本)",
            },
            {
                "name": "查找",
                "signature": "查找(文本, 片段)",
                "params": ("文本", "片段"),
                "note": "片段在文本里的位置，从 1 起。找不到是 0。",
                "kind": "expr",
                "snippet": "查找(文本, 片段)",
            },
            {
                "name": "提取数字",
                "signature": "提取数字(文本)",
                "params": ("文本",),
                "note": "从文字里抽出第一个数字。没有就返回 0。",
                "kind": "expr",
                "snippet": "提取数字(文本)",
            },
        ),
    },
    {
        "title": "大漠内存",
        "items": (
            {"name": "大漠内存.模块基址", "signature": "大漠内存.模块基址(模块)", "params": (('模块',),), "param_help": {'模块': '目标进程内已加载的模块文件名，例如“目标.exe”；不是磁盘路径'}, "note": "模块：目标进程内已加载的模块文件名，不是路径；返回加载基址整数。", "kind": "expr", "snippet": '大漠内存.模块基址("目标.exe")'},
            {"name": "大漠内存.模块大小", "signature": "大漠内存.模块大小(模块)", "params": (('模块',),), "param_help": {'模块': '目标进程内已加载的模块文件名，例如“目标.exe”；不是磁盘路径'}, "note": "返回指定模块的映像字节数。", "kind": "expr", "snippet": '大漠内存.模块大小("目标.exe")'},
            {"name": "大漠内存.读取整数", "signature": "大漠内存.读取整数(地址, 类型=0)", "params": (('地址', '类型=0'),), "param_help": {'地址': '绝对地址或“模块名+十六进制偏移”', '类型': '大漠原生整数类型编号'}, "note": "地址：绝对地址或“模块名+偏移”；类型：大漠原生整数类型编号。", "kind": "expr", "snippet": '大漠内存.读取整数("目标.exe+1000", 类型=0)'},
            {"name": "大漠内存.读取单精度", "signature": "大漠内存.读取单精度(地址)", "params": (('地址',),), "param_help": {'地址': '绝对地址或“模块名+十六进制偏移”'}, "note": "读取并返回 32 位单精度浮点数。", "kind": "expr", "snippet": '大漠内存.读取单精度("目标.exe+1000")'},
            {"name": "大漠内存.读取双精度", "signature": "大漠内存.读取双精度(地址)", "params": (('地址',),), "param_help": {'地址': '绝对地址或“模块名+十六进制偏移”'}, "note": "读取并返回 64 位双精度浮点数。", "kind": "expr", "snippet": '大漠内存.读取双精度("目标.exe+1000")'},
            {"name": "大漠内存.读取文本", "signature": "大漠内存.读取文本(地址, 类型, 长度)", "params": (('地址', '类型', '长度'),), "param_help": {'地址': '绝对地址或“模块名+十六进制偏移”', '类型': '大漠原生文本编码类型编号', '长度': '最大读取长度，必须大于 0'}, "note": "类型：大漠原生文本编码编号；长度：最大读取长度。", "kind": "expr", "snippet": '大漠内存.读取文本("目标.exe+1000", 0, 32)'},
            {"name": "大漠内存.读取数据", "signature": "大漠内存.读取数据(地址, 长度)", "params": (('地址', '长度'),), "param_help": {'地址': '绝对地址或“模块名+十六进制偏移”', '长度': '读取的字节数，必须大于 0'}, "note": "读取指定长度字节并返回十六进制文本。", "kind": "expr", "snippet": '大漠内存.读取数据("目标.exe+1000", 16)'},
            {"name": "大漠内存.写入整数", "signature": "大漠内存.写入整数(地址, 类型, 值)", "params": (('地址', '类型', '值'),), "param_help": {'地址': '绝对地址或“模块名+十六进制偏移”', '类型': '大漠原生整数类型编号', '值': '要写入的整数'}, "note": "按大漠原生类型编号写入整数，返回可检查“.通过”的动作结果。", "snippet": '大漠内存.写入整数("目标.exe+1000", 0, 1)'},
            {"name": "大漠内存.写入单精度", "signature": "大漠内存.写入单精度(地址, 值)", "params": (('地址', '值'),), "param_help": {'地址': '绝对地址或“模块名+十六进制偏移”', '值': '要写入的单精度小数'}, "note": "写入 32 位单精度浮点数，返回动作结果。", "snippet": '大漠内存.写入单精度("目标.exe+1000", 1.0)'},
            {"name": "大漠内存.写入双精度", "signature": "大漠内存.写入双精度(地址, 值)", "params": (('地址', '值'),), "param_help": {'地址': '绝对地址或“模块名+十六进制偏移”', '值': '要写入的双精度小数'}, "note": "写入 64 位双精度浮点数，返回动作结果。", "snippet": '大漠内存.写入双精度("目标.exe+1000", 1.0)'},
            {"name": "大漠内存.写入文本", "signature": "大漠内存.写入文本(地址, 类型, 值)", "params": (('地址', '类型', '值'),), "param_help": {'地址': '绝对地址或“模块名+十六进制偏移”', '类型': '大漠原生文本编码类型编号', '值': '要写入的文本'}, "note": "按大漠原生文本类型编号写入文本，返回动作结果。", "snippet": '大漠内存.写入文本("目标.exe+1000", 0, "文本")'},
            {"name": "大漠内存.写入数据", "signature": "大漠内存.写入数据(地址, 数据)", "params": (('地址', '数据'),), "param_help": {'地址': '绝对地址或“模块名+十六进制偏移”', '数据': '空格分隔的十六进制字节，例如“48 8B 01”'}, "note": "写入空格分隔的十六进制字节文本，返回动作结果。", "snippet": '大漠内存.写入数据("目标.exe+1000", "90 90")'},
            {"name": "大漠内存.搜索整数", "signature": "大漠内存.搜索整数(范围, 最小值, 最大值, 类型=0)", "params": (('范围', '最小值', '最大值', '类型=0'),), "param_help": {'范围': '“起始地址-结束地址”', '最小值': '搜索闭区间的最小整数', '最大值': '搜索闭区间的最大整数', '类型': '大漠原生整数类型编号'}, "note": "范围：“起始地址-结束地址”；最小值/最大值：闭区间；类型：原生编号。", "kind": "expr", "snippet": '大漠内存.搜索整数("00000000-7fffffff", 1, 1, 类型=0)'},
            {"name": "大漠内存.搜索单精度", "signature": "大漠内存.搜索单精度(范围, 最小值, 最大值)", "params": (('范围', '最小值', '最大值'),), "param_help": {'范围': '“起始地址-结束地址”', '最小值': '搜索闭区间的最小小数', '最大值': '搜索闭区间的最大小数'}, "note": "在显式地址范围内搜索单精度浮点数区间。", "kind": "expr", "snippet": '大漠内存.搜索单精度("00000000-7fffffff", 1.0, 1.0)'},
            {"name": "大漠内存.搜索双精度", "signature": "大漠内存.搜索双精度(范围, 最小值, 最大值)", "params": (('范围', '最小值', '最大值'),), "param_help": {'范围': '“起始地址-结束地址”', '最小值': '搜索闭区间的最小小数', '最大值': '搜索闭区间的最大小数'}, "note": "在显式地址范围内搜索双精度浮点数区间。", "kind": "expr", "snippet": '大漠内存.搜索双精度("00000000-7fffffff", 1.0, 1.0)'},
            {"name": "大漠内存.搜索文本", "signature": "大漠内存.搜索文本(范围, 文本, 类型)", "params": (('范围', '文本', '类型'),), "param_help": {'范围': '“起始地址-结束地址”', '文本': '要搜索的文本', '类型': '大漠原生文本编码类型编号'}, "note": "按大漠原生文本类型编号搜索文本并返回地址结果。", "kind": "expr", "snippet": '大漠内存.搜索文本("00000000-7fffffff", "文本", 0)'},
            {"name": "大漠内存.搜索数据", "signature": "大漠内存.搜索数据(范围, 数据)", "params": (('范围', '数据'),), "param_help": {'范围': '“起始地址-结束地址”', '数据': '空格分隔的十六进制字节序列'}, "note": "搜索空格分隔的十六进制字节序列并返回地址结果。", "kind": "expr", "snippet": '大漠内存.搜索数据("00000000-7fffffff", "90 90")'},
            {"name": "大漠内存.申请", "signature": "大漠内存.申请(地址, 大小, 类型)", "params": (('地址', '大小', '类型'),), "param_help": {'地址': '建议地址；“0”表示不指定', '大小': '申请的字节数，必须大于 0', '类型': '大漠原生内存申请类型编号'}, "note": "地址：“0”或建议地址；大小：字节数；类型：大漠原生申请类型编号。", "kind": "expr", "snippet": '大漠内存.申请("0", 4096, 0)'},
            {"name": "大漠内存.释放", "signature": "大漠内存.释放(地址)", "params": (('地址',),), "param_help": {'地址': '大漠内存.申请返回的地址'}, "note": "释放申请接口返回的目标内存地址，返回动作结果。", "snippet": '大漠内存.释放(地址)'},
            {"name": "大漠内存.清理", "signature": "大漠内存.清理()", "params": ((),), "param_help": {}, "note": "调用大漠目标进程内存清理接口，返回动作结果。", "snippet": '大漠内存.清理()'},
        ),
    },
    {
        "title": "大漠汇编",
        "items": (
            {"name": "大漠汇编.添加", "signature": "大漠汇编.添加(指令)", "params": (('指令',),), "param_help": {'指令': '一条大漠汇编器支持的汇编指令'}, "note": "向插件侧汇编队列末尾添加一条指令，返回动作结果。", "snippet": '大漠汇编.添加("mov eax,1")'},
            {"name": "大漠汇编.清空", "signature": "大漠汇编.清空()", "params": ((),), "param_help": {}, "note": "清空插件侧汇编指令队列；每次组装前应先调用。", "snippet": '大漠汇编.清空()'},
            {"name": "大漠汇编.调用", "signature": "大漠汇编.调用(模式)", "params": (('模式',),), "param_help": {'模式': '大漠原生汇编调用模式编号'}, "note": "按大漠原生模式编号执行当前汇编队列并返回执行结果。", "kind": "expr", "snippet": '大漠汇编.调用(0)'},
            {"name": "大漠汇编.基址调用", "signature": "大漠汇编.基址调用(模式, 基址)", "params": (('模式', '基址'),), "param_help": {'模式': '大漠原生汇编调用模式编号', '基址': '汇编代码使用的基准地址'}, "note": "按指定基址和大漠原生模式执行汇编队列。", "kind": "expr", "snippet": '大漠汇编.基址调用(0, "0")'},
            {"name": "大漠汇编.超时", "signature": "大漠汇编.超时(毫秒, 参数)", "params": (('毫秒', '参数'),), "param_help": {'毫秒': '非负的超时时间', '参数': '大漠汇编超时接口的原生控制参数'}, "note": "毫秒：非负超时时间；参数：大漠原生控制编号。", "snippet": '大漠汇编.超时(5000, 0)'},
        ),
    },
    {
        "title": "编程工具",
        "items": (
            {"name": "JSON解析", "signature": "JSON解析(文本)", "params": (("文本",),), "note": "将 JSON 文本解析为列表或字典。", "kind": "expr", "snippet": "JSON解析(文本)"},
            {"name": "JSON生成", "signature": "JSON生成(对象)", "params": (("对象",),), "note": "将列表或字典生成 JSON 文本。", "kind": "expr", "snippet": "JSON生成(对象)"},
            {"name": "字典获取", "signature": "字典获取(字典, 键, 默认值=None)", "params": (("字典", "键", "默认值=None"),), "note": "安全读取字典键。", "kind": "expr", "snippet": "字典获取(字典, 键, 默认值=None)"},
            {"name": "字典设置", "signature": "字典设置(字典, 键, 值)", "params": (("字典", "键", "值"),), "note": "写入字典键并返回字典。", "kind": "expr", "snippet": "字典设置(字典, 键, 值)"},
            {"name": "列表追加", "signature": "列表追加(列表, 值)", "params": (("列表", "值"),), "note": "向列表末尾追加一项。", "kind": "expr", "snippet": "列表追加(列表, 值)"},
            {"name": "列表合并", "signature": "列表合并(列表, 数据)", "params": (("列表", "数据"),), "note": "批量追加列表数据。", "kind": "expr", "snippet": "列表合并(列表, 数据)"},
            {"name": "列表弹出", "signature": "列表弹出(列表, 索引=-1)", "params": (("列表", "索引=-1"),), "note": "移除并返回列表项目。", "kind": "expr", "snippet": "列表弹出(列表, 索引=-1)"},
            {"name": "字典键", "signature": "字典键(字典)", "params": (("字典",),), "note": "返回字典的键列表。", "kind": "expr", "snippet": "字典键(字典)"},
            {"name": "字典值", "signature": "字典值(字典)", "params": (("字典",),), "note": "返回字典的值列表。", "kind": "expr", "snippet": "字典值(字典)"},
            {"name": "类型", "signature": "类型(值)", "params": (("值",),), "note": "返回值的类型名称。", "kind": "expr", "snippet": "类型(值)"},
            {"name": "开头是", "signature": "开头是(文本, 前缀)", "params": (("文本", "前缀"),), "note": "判断文本是否以指定前缀开头。", "kind": "expr", "snippet": "开头是(文本, 前缀)"},
            {"name": "结尾是", "signature": "结尾是(文本, 后缀)", "params": (("文本", "后缀"),), "note": "判断文本是否以指定后缀结尾。", "kind": "expr", "snippet": "结尾是(文本, 后缀)"},
            {"name": "断言", "signature": "断言(条件, 错误信息=\"断言失败\")", "params": (("条件", "错误信息=\"断言失败\""),), "note": "条件不成立时立即报告脚本错误。", "kind": "expr", "snippet": "断言(条件, 错误信息=\"断言失败\")"},
            {"name": "枚举", "signature": "枚举(列表)", "params": (("列表",),), "note": "同时取得序号和项目。", "kind": "expr", "snippet": "枚举(列表)"},
            {"name": "配对", "signature": "配对(列表1, 列表2)", "params": (("列表1", "列表2"),), "note": "按位置配对多个序列。", "kind": "expr", "snippet": "配对(列表1, 列表2)"},
            {"name": "排序", "signature": "排序(列表)", "params": (("列表",),), "note": "返回排序后的新列表。", "kind": "expr", "snippet": "排序(列表)"},
            {"name": "四舍五入", "signature": "四舍五入(数值, 位数=0)", "params": (("数值", "位数=0"),), "note": "按指定小数位四舍五入。", "kind": "expr", "snippet": "四舍五入(数值)"},
            {"name": "全部", "signature": "全部(条件列表)", "params": (("条件列表",),), "note": "判断列表是否全部为真。", "kind": "expr", "snippet": "全部(条件列表)"},
            {"name": "任一", "signature": "任一(条件列表)", "params": (("条件列表",),), "note": "判断列表是否至少一项为真。", "kind": "expr", "snippet": "任一(条件列表)"},
        ),
    },
)


def iter_script_insert_items() -> List[Dict[str, Any]]:
    items: List[Dict[str, Any]] = []
    seen_names = set()
    for group in SCRIPT_INSERT_GROUPS:
        for item in group.get("items") or ():
            if not isinstance(item, dict) or not str(item.get("snippet") or "").strip():
                continue
            name = str(item.get("name") or "").strip()
            if not name or name in seen_names:
                continue
            seen_names.add(name)
            items.append(item)
    return items


# 从插入目录签名里抽取“名字(...)”调用路径的正则，与测试用例保持同一份，
# 供生产（命令补全同步）与测试（目录/文档一致性）共用，避免手工维护漂移。
_CATALOG_CALL_RE = re.compile(
    r"(?<![\w一-鿿])([A-Za-z_一-鿿][\w一-鿿]*(?:\.[A-Za-z_一-鿿][\w一-鿿]*)*)\s*\("
)


def script_catalog_call_paths(include_result_methods: bool = True) -> List[str]:
    """返回插入目录里所有命令/命名空间方法的调用路径（去重、保持出现顺序）。

    忽略 ``子程序``/``函数`` 这类“声明”条目（其括号里的标识符是用户自定义名字，
    不是命令）。``include_result_methods=False`` 时进一步剔除结果对象方法
    （``点``/``随机点`` 这类以结果变量点出来的方法），只留独立命令，供命令补全
    同步与文档一致性检查使用。
    """
    from task_workflow.script_sandbox import _RESULT_METHODS

    paths: List[str] = []
    for item in iter_script_insert_items():
        if str(item.get("name") or "").strip() in {"子程序", "函数"}:
            continue
        for path in _CATALOG_CALL_RE.findall(str(item.get("signature") or "")):
            if not include_result_methods and path.split(".")[-1] in _RESULT_METHODS:
                continue
            if path not in paths:
                paths.append(path)
    return paths


def script_completion_names() -> List[str]:
    names = [str(item.get("name") or "") for item in iter_script_insert_items()]
    names.extend(
        (
            "变量.获取",
            "变量.设置",
            "变量.增加",
            "变量.全局获取",
            "变量.全局设置",
            "变量.全局增加",
            "上次.分数",
            "上次.x",
            "上次.y",
            "上次.通过",
            "文字.内容",
            "文字.通过",
            "找图.分数",
            "找图.路径",
            "找图.x",
            "找图.y",
            "找图.通过",
            "检测.类别",
            "检测.分数",
            "检测.列表",
            "检测.x",
            "检测.y",
            "检测.宽",
            "检测.高",
            "检测.通过",
            "卡片",
            "否则如果",
            "否则",
            "中断",
            "继续",
            "并且",
            "或者",
            "不是",
            "真",
            "假",
            "空",
            "长度",
            "整数",
            "小数",
            "到文本",
            "真假",
            "最小",
            "最大",
            "绝对值",
            "开方",
            "平方根",
            "角度",
            "正弦",
            "余弦",
            "限制",
            "距离",
            "范围",
            "随机",
            "包含",
            "截取",
            "替换",
            "分割",
            "去空格",
            "查找",
            "提取数字",
            "时间",
            "窗口.宽",
            "窗口.高",
            "窗口.设置分辨率",
            "窗口.激活",
            "剪贴板.获取",
            "剪贴板.设置",
            "组件.加载",
            "组件.调用",
            "组件.读取",
            "组件.写入",
            "组件.运行",
            "组件.关闭",
            "变量.存在",
            "变量.删除",
            "变量.全局存在",
            "变量.全局删除",
            "数据.读取",
            "数据.写入",
            "数据.JSON读取",
            "数据.JSON写入",
            "数据.存在",
            "数据.删除",
            "结果.通过",
            "结果.x",
            "结果.y",
            "结果.宽",
            "结果.高",
            "结果.分数",
            "结果.类别",
            "结果.内容",
            "结果.列表",
            "色.颜色",
            "色.红",
            "色.绿",
            "色.蓝",
            "色.x",
            "色.y",
            "色.通过",
        )
    )
    return sorted({name for name in names if name})


_PLACEHOLDER_TOKENS = (
    "区域=(x, y, 宽, 高)",
    "区域=(x, y, 宽, 高)",
    '目标="金币"',
    '类别="敌人"',
    '键="右键"',
    '键="左键"',
    '方向="向下"',
    '方向="向上"',
    "阈值=0.8",
    "阈值=0.5",
    '策略="最近"',
    '策略="最大"',
    '策略="置信度最高"',
    "横向=0.5",
    "纵向=0.85",
    "纵向=0.5",
    "边距=2",
    "点击=假",
    "点击=真",
    "点击=False",
    "点击=True",
    "报错=假",
    "报错=真",
    "双击=假",
    "双击=真",
    "偏移x=0",
    "偏移y=0",
    "随机=0",
    "步数=3",
    "步长=1",
    "超时=8",
    "间隔=0.3",
    "间隔=0.08",
    "偏色=20",
    "最多=20",
    "秒=0.5",
    "秒=(0.2, 0.6)",
    '动作="按下"',
    '动作="松开"',
    "次数=3",
    "默认值",
    "起点横坐标",
    "起点纵坐标",
    "终点横坐标",
    "终点纵坐标",
    "横坐标",
    "纵坐标",
    "条件",
    "次数",
    "超时",
    "间隔",
    "偏色",
    "最多",
    "毫秒",
    "片段",
    "旧",
    "新",
    "分隔符",
    "字数",
    "图片",
    "模型",
    "文件",
    "等待",
    "颜色",
    "文本",
    "目标",
    "名称",
    "说明",
    "内容",
    "编号",
    "名字",
    "对象",
    "值1",
    "值2",
    "点1",
    "点2",
    "下限",
    "上限",
    "起点",
    "终点",
    "值",
    "秒",
    "按键",
    "步数",
    "横向",
    "纵向",
    "边距",
    "自己",
    "策略",
    "类别",
    "宽",
    "高",
    "x1",
    "y1",
    "x2",
    "y2",
    "x",
    "y",
)


def iter_placeholder_spans(text: str) -> List[Tuple[int, int]]:
    source = str(text or "")
    used = [False] * len(source)
    spans: List[Tuple[int, int]] = []
    for token in _PLACEHOLDER_TOKENS:
        start = 0
        while True:
            index = _find_token(source[start:], token)
            if index < 0:
                break
            index += start
            end = index + len(token)
            if not any(used[index:end]):
                spans.append((index, end))
                for pos in range(index, end):
                    used[pos] = True
            start = index + 1
    spans.sort()
    return spans


def first_placeholder_span(text: str) -> Optional[Tuple[int, int]]:
    source = str(text or "")
    empty_quotes = source.find('""')
    if empty_quotes >= 0:
        return empty_quotes, empty_quotes + 2
    empty_single = source.find("''")
    if empty_single >= 0:
        return empty_single, empty_single + 2
    spans = iter_placeholder_spans(source)
    if spans:
        return spans[0]
    empty_call = source.find("()")
    if empty_call >= 0:
        return empty_call + 1, empty_call + 1
    for quote in ('"', "'"):
        start = source.find(quote)
        if start < 0:
            continue
        end = source.find(quote, start + 1)
        if end > start:
            return start, end + 1
    return None


def next_placeholder_span(text: str, column: int, backward: bool = False) -> Optional[Tuple[int, int]]:
    spans = iter_placeholder_spans(text)
    if not spans:
        return None
    cursor = max(0, int(column))
    if backward:
        for start, end in reversed(spans):
            if end <= cursor:
                return start, end
        return None
    for start, end in spans:
        if start >= cursor:
            return start, end
    return None


def _find_token(text: str, token: str) -> int:
    start = 0
    while True:
        index = text.find(token, start)
        if index < 0:
            return -1
        before = text[index - 1] if index > 0 else ""
        after = text[index + len(token)] if index + len(token) < len(text) else ""
        if not _is_ident_char(before) and not _is_ident_char(after):
            return index
        start = index + 1


def _is_ident_char(char: str) -> bool:
    if not char:
        return False
    return char.isalnum() or char == "_" or "\u4e00" <= char <= "\u9fff"


def leading_whitespace(text: str) -> str:
    index = 0
    while index < len(text) and text[index] in {" ", "\t"}:
        index += 1
    return text[:index].replace("\t", "    ")


def source_lines(source: Any) -> List[str]:
    """按 QTextDocument 的块切分源码，保留结尾空行，索引与文档块一一对应。

    ``str.splitlines()`` 会丢掉结尾换行造成的空行，于是光标停在文档最后那个空块时，
    行号会比实际块索引大 1，插入/改写就误落到上一行。这里用显式 ``split("\\n")``
    保留结尾空行；结果永不为空（空串返回 ``[""]``）。
    """
    text = str(source or "").replace("\r\n", "\n").replace("\r", "\n")
    return text.split("\n")


def indent_snippet(snippet: str, indent: str) -> str:
    lines = str(snippet or "").splitlines() or [""]
    return "\n".join(indent + line for line in lines)


def _block_body_end(lines: List[str], header_index: int) -> int:
    header_indent = leading_whitespace(lines[header_index])
    end = header_index + 1
    while end < len(lines):
        line = lines[end]
        if not line.strip():
            break
        if len(leading_whitespace(line)) <= len(header_indent):
            break
        end += 1
    return end - 1


def _indent_from_above(lines: List[str], line_index: int) -> str:
    for previous in reversed(lines[:line_index]):
        if not previous.strip():
            continue
        indent = leading_whitespace(previous)
        if previous.rstrip().endswith(":"):
            return indent + "    "
        return indent
    return ""


def command_matches_query(item: Dict[str, Any], query: str) -> bool:
    needle = str(query or "").strip().lower()
    if not needle:
        return True
    haystack = " ".join(
        (
            str((item or {}).get("name") or ""),
            str((item or {}).get("signature") or ""),
            str((item or {}).get("note") or ""),
            str((item or {}).get("snippet") or ""),
            command_example_text(item or {}),
        )
    ).lower()
    return needle in haystack


def _is_command_name_char(char: str) -> bool:
    return _is_ident_char(char) or char == "."


def command_name_from_snippet(snippet: str) -> str:
    text = str(snippet or "").strip()
    index = 0
    while index < len(text) and _is_command_name_char(text[index]):
        index += 1
    return text[:index]


def call_name_before(text: str, open_index: int) -> str:
    source = str(text or "")
    index = max(0, min(int(open_index), len(source)))
    while index > 0 and source[index - 1].isspace():
        index -= 1
    end = index
    while index > 0 and _is_command_name_char(source[index - 1]):
        index -= 1
    return source[index:end]


def ident_span(line: str, column: int) -> Tuple[int, int]:
    text = str(line or "")
    position = max(0, min(int(column), len(text)))
    start = position
    while start > 0 and _is_command_name_char(text[start - 1]):
        start -= 1
    end = position
    while end < len(text) and _is_command_name_char(text[end]):
        end += 1
    return start, end


def _is_block_header(line: str) -> bool:
    return bool(str(line or "").rstrip().endswith(":"))


def _is_outdent_keyword(name: str) -> bool:
    return str(name or "") in {"else", "elif", "否则", "否则如果"}


def _outer_indent(lines: List[str], index: int) -> str:
    current = lines[index] if 0 <= index < len(lines) else ""
    current_indent = leading_whitespace(current) if current.strip() else (_indent_from_above(lines, index) or "")
    for previous in reversed(lines[: max(0, index)]):
        if not previous.strip():
            continue
        indent = leading_whitespace(previous)
        if _is_block_header(previous) and len(indent) <= len(current_indent):
            return indent
        if len(indent) < len(current_indent):
            return indent
    return ""


def _should_complete_token(token: str, name: str, line: str) -> bool:
    command = str(name or "").strip()
    piece = str(token or "").strip()
    if not piece or not command:
        return False
    if piece == command:
        return str(line or "").strip() == piece
    if command.startswith(piece):
        return True
    tail = command.split(".")[-1]
    return bool(tail) and tail.startswith(piece)


_STMT_COMMANDS = frozenset(
    {
        "移动",
        "拖拽",
        "滚轮",
        "按键",
        "输入",
        "延时",
        "等毫秒",
        "相对移动",
        "按下",
        "松开",
        "按住",
        "连点",
        "窗口.设置分辨率",
        "窗口.激活",
        "播放",
        "停止播放",
        "回放",
        "暂停回放",
        "继续回放",
        "停止回放",
        "剪贴板.设置",
        "记录",
        "成功",
        "失败",
        "变量.设置",
        "变量.全局设置",
        "区域.设置",
        "区域.删除",
        "如果",
        "循环",
        "当",
        "否则如果",
        "否则",
        "中断",
        "继续",
        "if",
        "for",
        "while",
        "elif",
        "else",
    }
)
_RESULT_ASSIGN_PREFIXES = (
    "找图(",
    "等图(",
    "找所有图(",
    "检测(",
    "等检测(",
    "等检测消失(",
    "框内点(",
    "随机点(",
    "找字(",
    "找字库(",
    "等字库(",
    "等字库消失(",
    "点字库(",
    "等文字(",
    "等图消失(",
    "等色消失(",
    "等文字消失(",
    "鼠标位置(",
    "找色(",
    "等色(",
    "取色(",
    "比色(",
    "点文字(",
    "点元素(",
    "截图(",
    "等按键(",
)
_HEADER_PREFIXES = (
    ("否则如果 ", "条件"),
    ("如果 ", "条件"),
    ("当 ", "条件"),
    ("elif ", "条件"),
    ("if ", "条件"),
    ("while ", "条件"),
)
_CARD_INDEX_RE = re.compile(r"卡片\[([^\]]*)\]")
_LOOP_RANGE_TOKENS = ("范围(",)
_PARAM_CHOICES = {
    "点击": ("假", "真"),
    "双击": ("假", "真"),
    "等待": ("真", "假"),
    "键": ('"左键"', '"右键"'),
    "方向": ('"向下"', '"向上"'),
    "动作": ('"按下"', '"松开"'),
    "方式": ('"粘贴"', '"仿真"'),
    "策略": ('"最近"', '"最大"', '"置信度最高"'),
}


def insert_item_catalog() -> Dict[str, Dict[str, Any]]:
    return {str(item.get("name") or ""): item for item in iter_script_insert_items() if item.get("name")}


def catalog_snippet(name: str) -> str:
    item = insert_item_catalog().get(str(name or ""))
    return str((item or {}).get("snippet") or "").rstrip()


def command_kind(name: str, item: Optional[Dict[str, Any]] = None) -> str:
    if item and item.get("kind"):
        return str(item.get("kind") or "stmt")
    catalog = insert_item_catalog().get(str(name or "")) or {}
    if catalog.get("kind"):
        return str(catalog.get("kind") or "stmt")
    return "stmt" if str(name or "") in _STMT_COMMANDS else "expr"


def command_returns_label(name: str) -> str:
    return "结果" if command_kind(name) == "expr" else "无"


def _is_concrete_snippet(snippet: str) -> bool:
    text = str(snippet or "")
    if '"' in text or "'" in text:
        return True
    stripped = text
    for token in _PLACEHOLDER_TOKENS:
        stripped = stripped.replace(token, "")
    return any(char.isdigit() for char in stripped)


def is_placeholder_text(text: str) -> bool:
    value = str(text or "").strip()
    if not value:
        return True
    for token in _PLACEHOLDER_TOKENS:
        if value == token:
            return True
    key = value.split("=", 1)[0].strip()
    return key in {
        "条件",
        "次数",
        "图片",
        "颜色",
        "目标",
        "文本",
        "内容",
        "说明",
        "名字",
        "名称",
        "编号",
        "按键",
        "秒",
        "动作",
        "值",
        "默认值",
        "步数",
        "步长",
        "类别",
        "阈值",
        "策略",
        "文件",
        "等待",
        "横向",
        "纵向",
        "边距",
        "自己",
        "点击",
        "双击",
        "偏移x",
        "偏移y",
        "随机",
        "超时",
        "间隔",
        "偏色",
        "最多",
        "毫秒",
        "片段",
        "旧",
        "新",
        "分隔符",
        "字数",
        "键",
        "方向",
        "区域",
        "对象",
        "值1",
        "值2",
        "点1",
        "点2",
        "角度",
        "下限",
        "上限",
        "起点",
        "终点",
        "横坐标",
        "纵坐标",
        "起点横坐标",
        "起点纵坐标",
        "终点横坐标",
        "终点纵坐标",
        "x",
        "y",
        "x1",
        "y1",
        "x2",
        "y2",
    }


def wrap_first_arg(snippet: str, value: str) -> str:
    text = str(snippet or "").rstrip()
    filled = str(value or "").strip()
    if not filled:
        return text
    span = first_placeholder_span(text)
    if span:
        return text[: span[0]] + filled + text[span[1] :]
    empty = text.find("()")
    if empty >= 0:
        return f"{text[: empty + 1]}{filled}{text[empty + 1 :]}"
    return text


def _nearest_result_var(lines: List[str], index: int) -> Optional[str]:
    for line in reversed(lines[: index + 1]):
        stripped = str(line or "").strip()
        if not stripped or stripped.startswith(
            ("如果 ", "否则如果 ", "当 ", "循环 ", "if ", "elif ", "while ", "for ", "否则:", "else:")
        ):
            continue
        if "=" not in stripped:
            continue
        left, right = stripped.split("=", 1)
        name = left.strip()
        right = right.lstrip()
        if name and any(right.startswith(prefix) for prefix in _RESULT_ASSIGN_PREFIXES):
            return name
    return None


def statement_for_insert(name: str, snippet: str, result_var: Optional[str] = None, header: str = "") -> str:
    command = str(name or "").strip()
    text = str(snippet or "").rstrip()
    if not _is_concrete_snippet(text):
        text = catalog_snippet(command) or text
    if command == "点击":
        if result_var:
            return f"点击({result_var}, 双击=假, 偏移x=0, 偏移y=0, 随机=0)"
        if any(token in str(header or "") for token in ("找图", "检测", "找字", "找字库", "找色", "点文字", "点字库", "点元素")):
            return "点击()"
        return text or "点击(目标, 双击=假, 偏移x=0, 偏移y=0, 随机=0)"
    if command == "移动" and result_var:
        return f"移动({result_var})"
    if command == "拖拽" and result_var:
        return f"拖拽({result_var}, 终点)"
    if command == "滚轮" and result_var:
        return f'滚轮({result_var}, 方向="向下")'
    if command == "框内点" and result_var:
        return f"框内点({result_var}, 横向=0.5, 纵向=0.5)"
    if command == "随机点" and result_var:
        return f"随机点({result_var}, 边距=2)"
    if command in {"如果", "if"} and result_var:
        return f"{command} {result_var}:"
    if command in {"当", "while"} and result_var:
        return f"{command} {result_var}:"
    return text


def _placeholder_span_at(
    line: str,
    column: int,
    select_start: Optional[int] = None,
    select_end: Optional[int] = None,
) -> Optional[Tuple[int, int]]:
    spans = iter_placeholder_spans(line)
    if select_start is not None and select_end is not None and select_end > select_start:
        for start, end in spans:
            if start <= select_start and select_end <= end:
                return start, end
            if select_start <= start and end <= select_end:
                return start, end
    for start, end in spans:
        if start <= column <= end:
            return start, end
    return None


def _empty_call_span_at(line: str, open_index: int) -> Optional[Tuple[int, int, str]]:
    text = str(line or "")
    if open_index < 0 or open_index >= len(text) or text[open_index] != "(":
        return None
    close_index = text.find(")", open_index + 1)
    if close_index < 0 or text[open_index + 1 : close_index].strip():
        return None
    name = command_name_from_snippet(text[:open_index])
    name = name.split()[-1] if name else ""
    if not name:
        return None
    return open_index + 1, close_index, name


def _empty_call_at_cursor(line: str, column: int) -> Optional[Tuple[int, int]]:
    text = str(line or "")
    position = max(0, min(int(column), len(text)))
    open_index = text.rfind("(", 0, position + 1)
    found = _empty_call_span_at(text, open_index)
    if found and found[0] <= position <= found[1]:
        return found[0], found[1]
    return None


def _nest_target(
    line: str,
    column: int,
    select_start: Optional[int] = None,
    select_end: Optional[int] = None,
) -> Optional[Tuple[int, int]]:
    if select_start is not None and select_end is not None and select_end > select_start:
        return select_start, select_end
    placeholder = _placeholder_span_at(line, column, select_start, select_end)
    if placeholder:
        return placeholder
    empty = _empty_call_at_cursor(line, column)
    if empty:
        return empty
    return None


def _match_paren(text: str, open_index: int) -> Optional[int]:
    depth = 0
    quote = None
    escape = False
    for index in range(open_index, len(text)):
        char = text[index]
        if quote:
            if escape:
                escape = False
            elif char == "\\":
                escape = True
            elif char == quote:
                quote = None
            continue
        if char in {'"', "'"}:
            quote = char
            continue
        if char == "(":
            depth += 1
            continue
        if char == ")":
            depth -= 1
            if depth == 0:
                return index
    return None


def _split_args_spans(text: str, start: int, end: int) -> List[Tuple[int, int, str]]:
    args: List[Tuple[int, int, str]] = []
    buf_start = start
    depth = 0
    quote = None
    for index in range(start, end):
        char = text[index]
        if quote:
            if char == quote:
                quote = None
            continue
        if char in {'"', "'"}:
            quote = char
            continue
        if char in "([{":
            depth += 1
            continue
        if char in ")]}":
            depth = max(0, depth - 1)
            continue
        if char == "," and depth == 0:
            token = text[buf_start:index].strip()
            if token:
                lead = 0
                while buf_start + lead < index and text[buf_start + lead] in {" ", "\t"}:
                    lead += 1
                args.append((buf_start + lead, buf_start + lead + len(token), token))
            buf_start = index + 1
    token = text[buf_start:end].strip()
    if token:
        lead = 0
        while buf_start + lead < end and text[buf_start + lead] in {" ", "\t"}:
            lead += 1
        args.append((buf_start + lead, buf_start + lead + len(token), token))
    return args


def _normalize_overloads(params: Any) -> List[Tuple[str, ...]]:
    if not params:
        return []
    first = params[0]
    if isinstance(first, (tuple, list)):
        return [tuple(str(part) for part in item) for item in params if item]
    return [tuple(str(part) for part in params)]


def _looks_like_xy_token(raw: str) -> bool:
    text = str(raw or "").strip()
    if not text:
        return False
    if _field_label(text) in {"横坐标", "纵坐标", "x", "y"}:
        return True
    if text.startswith(("+", "-")):
        text = text[1:]
    return bool(text) and text.isdigit()


def _arg_raw(arg: Any) -> str:
    if isinstance(arg, (tuple, list)) and len(arg) >= 3:
        return str(arg[2] or "").strip()
    return str(arg or "").strip()


def _arg_keyword(raw: str) -> str:
    text = str(raw or "").strip()
    if "=" not in text or text.startswith(("'", '"', "(")):
        return ""
    return text.split("=", 1)[0].strip()


def _looks_like_number_token(raw: str) -> bool:
    text = str(raw or "").strip()
    if not text or text.startswith(("'", '"', "(")):
        return False
    try:
        float(text)
    except ValueError:
        return False
    return True


def _bind_call_args(
    specs: Sequence[str],
    args: List,
) -> Tuple[List[Tuple[str, Any]], List, Dict[str, Any]]:
    positionals: List = []
    keywords: Dict[str, Any] = {}
    for arg in args:
        key = _arg_keyword(_arg_raw(arg))
        if key:
            keywords[key] = arg
        else:
            positionals.append(arg)
    bound: List[Tuple[str, Any]] = []
    pos_index = 0
    used_keywords = set()
    for spec in specs:
        label = _field_label(spec)
        if label in keywords:
            bound.append((spec, keywords[label]))
            used_keywords.add(label)
        elif "=" not in spec and pos_index < len(positionals):
            bound.append((spec, positionals[pos_index]))
            pos_index += 1
        else:
            bound.append((spec, None))
    leftover_pos = positionals[pos_index:]
    if leftover_pos and _looks_like_number_token(_arg_raw(leftover_pos[0])):
        for index, (spec, arg) in enumerate(bound):
            if arg is None and _field_label(spec) == "阈值":
                bound[index] = (spec, leftover_pos.pop(0))
                break
    leftover_kw = {key: arg for key, arg in keywords.items() if key not in used_keywords}
    return bound, leftover_pos, leftover_kw


def _overload_score(specs: Sequence[str], args: List) -> Optional[Tuple[int, ...]]:
    _bound, leftover_pos, leftover_kw = _bind_call_args(specs, args)
    if leftover_kw:
        return None
    unused_required = 0
    unused_optional = 0
    for spec, arg in _bound:
        if arg is not None:
            continue
        if "=" in spec:
            unused_optional += 1
        else:
            unused_required += 1
    return (len(leftover_pos), unused_required, unused_optional, len(specs))


def _first_positional_raw(args: List) -> str:
    for arg in args:
        if not _arg_keyword(_arg_raw(arg)):
            return _arg_raw(arg)
    return ""


def _preferred_overloads(items: List[Tuple[str, ...]], args: List) -> List[Tuple[str, ...]]:
    first = _first_positional_raw(args)
    if _looks_like_xy_token(first):
        group = [item for item in items if item and _field_label(item[0]) in {"横坐标", "x"}]
        if group:
            return group
    elif first:
        group = [item for item in items if item and _field_label(item[0]) == "目标"]
        if group:
            return group
    return items


def _pick_call_overload(overloads: List[Tuple[str, ...]], args: List) -> Tuple[str, ...]:
    items = [tuple(item) for item in (overloads or []) if item]
    if not items:
        return ()
    if not args:
        targets = [item for item in items if item and _field_label(item[0]) == "目标"]
        if targets:
            return max(targets, key=len)
        return items[0]
    pool = _preferred_overloads(items, args)
    scored = []
    for candidate in (pool, items):
        scored = [
            (score, item)
            for item in candidate
            if (score := _overload_score(item, args)) is not None
        ]
        if scored:
            scored.sort(key=lambda item: item[0])
            return scored[0][1]
    return pool[0] if pool else items[0]


def _is_image_label(label: str) -> bool:
    text = str(label or "")
    return text == "图片" or text.startswith("图片")


def _next_image_spec(used: set) -> str:
    index = 2
    while f"图片{index}" in used or (index == 1 and "图片" in used):
        index += 1
    return f"图片{index}"


def _make_call_field(index: int, spec: str, arg: Any, close_index: int, position: int) -> Dict[str, Any]:
    raw = _arg_raw(arg) if arg is not None else ""
    start, end = (int(arg[0]), int(arg[1])) if arg is not None else (close_index, close_index)
    return {
        "index": index,
        "label": _field_label(spec),
        "spec": spec,
        "raw": raw,
        "value": _field_display_value(spec, raw),
        "start": start,
        "end": end,
        "choices": _PARAM_CHOICES.get(_field_label(spec), ()),
        "active": bool(arg is not None and int(arg[0]) <= position <= int(arg[1])),
    }


def _call_fields(specs: Sequence[str], args: List, close_index: int, position: int) -> List[Dict[str, Any]]:
    bound, leftover_pos, leftover_kw = _bind_call_args(specs, args)
    fields = [_make_call_field(index, spec, arg, close_index, position) for index, (spec, arg) in enumerate(bound)]
    insert_at = 0
    used = set()
    for index, field in enumerate(fields):
        label = str(field.get("label") or "")
        used.add(label)
        if _is_image_label(label):
            insert_at = index + 1
    extras = []
    for arg in leftover_pos:
        spec = _next_image_spec(used)
        extras.append(_make_call_field(0, spec, arg, close_index, position))
        used.add(spec)
    if extras:
        fields[insert_at:insert_at] = extras
    for key, arg in leftover_kw.items():
        raw = _arg_raw(arg)
        spec = raw if "=" in raw else key
        fields.append(_make_call_field(len(fields), spec, arg, close_index, position))
    for index, field in enumerate(fields):
        field["index"] = index
    return fields


def _field_label(spec: str) -> str:
    return str(spec or "").split("=", 1)[0].strip()


def _field_default(spec: str) -> str:
    text = str(spec or "")
    if "=" not in text:
        return ""
    return text.split("=", 1)[1].strip()


def _field_display_value(spec: str, raw: str) -> str:
    text = str(raw or "").strip()
    key = _field_label(spec)
    prefix = f"{key}="
    if text.startswith(prefix):
        return text[len(prefix) :]
    if text:
        return text
    return _field_default(spec)


def format_param_value(spec: str, value: str) -> str:
    raw = str(value or "").strip()
    key = _field_label(spec)
    if "=" in str(spec or "") and raw and not raw.startswith(f"{key}="):
        return f"{key}={raw}"
    return raw


def parse_param_fields(line: str, column: int = 0) -> Optional[Dict[str, Any]]:
    text = str(line or "")
    position = max(0, min(int(column), len(text)))
    open_index = text.rfind("(", 0, position + 1)
    close_index = _match_paren(text, open_index) if open_index >= 0 else None
    if open_index >= 0 and close_index is not None and open_index < position <= close_index:
        name = call_name_before(text, open_index)
        item = insert_item_catalog().get(name) or {}
        overloads = _normalize_overloads(item.get("params"))
        args = _split_args_spans(text, open_index + 1, close_index)
        labels = _pick_call_overload(overloads, args)
        fields = _call_fields(labels, args, close_index, position)
        if not fields and name:
            fields.append(
                {
                    "index": 0,
                    "label": "",
                    "spec": "",
                    "raw": "",
                    "value": "",
                    "start": open_index + 1,
                    "end": close_index,
                    "choices": (),
                    "active": True,
                }
            )
        active = 0
        for index, field in enumerate(fields):
            if field.get("active"):
                active = index
                break
        return {
            "kind": "call",
            "name": name,
            "open": open_index,
            "close": close_index,
            "fields": fields,
            "active": active,
        }

    loop_fields = _parse_loop_header(text, position)
    if loop_fields:
        return loop_fields
    card_fields = _parse_card_index(text, position)
    if card_fields:
        return card_fields

    stripped = text.lstrip()
    for prefix, label in _HEADER_PREFIXES:
        if stripped.startswith(prefix) and stripped.endswith(":"):
            value = stripped[len(prefix) : -1]
            start = len(text) - len(stripped) + len(prefix)
            while start < len(text) and text[start] in {" ", "\t"}:
                start += 1
            end = len(text.rstrip()) - 1 if text.rstrip().endswith(":") else len(text)
            while end > start and text[end - 1] in {" ", "\t"}:
                end -= 1
            return {
                "kind": "header",
                "name": prefix.strip(),
                "fields": [
                    {
                        "index": 0,
                        "label": label,
                        "spec": label,
                        "raw": value.strip(),
                        "value": value.strip(),
                        "start": start,
                        "end": end,
                        "choices": (),
                        "active": True,
                    }
                ],
                "active": 0,
            }
    return None


def _parse_loop_header(line: str, column: int = 0) -> Optional[Dict[str, Any]]:
    text = str(line or "")
    stripped = text.lstrip()
    if not (stripped.startswith("循环 ") or stripped.startswith("for ")):
        return None
    for token in _LOOP_RANGE_TOKENS:
        index = text.find(token)
        if index < 0:
            continue
        open_index = index + len(token) - 1
        close_index = _match_paren(text, open_index)
        if close_index is None:
            continue
        parsed = parse_param_fields(text, open_index + 1)
        if parsed and parsed.get("kind") == "call":
            parsed["name"] = "循环" if stripped.startswith("循环 ") else "for"
            return parsed
    return None


def _parse_card_index(line: str, column: int = 0) -> Optional[Dict[str, Any]]:
    text = str(line or "")
    position = max(0, min(int(column), len(text)))
    match = _CARD_INDEX_RE.search(text)
    if match is None or not (match.start() <= position <= match.end()):
        return None
    return {
        "kind": "header",
        "name": "卡片",
        "fields": [
            {
                "index": 0,
                "label": "编号",
                "spec": "编号",
                "raw": match.group(1),
                "value": match.group(1),
                "start": match.start(1),
                "end": match.end(1),
                "choices": (),
                "active": True,
            }
        ],
        "active": 0,
    }


def apply_param_value(line: str, column: int, arg_index: int, value: str) -> str:
    parsed = parse_param_fields(line, column)
    if not parsed:
        return line
    fields = list(parsed.get("fields") or [])
    if not fields:
        return line
    index = max(0, min(int(arg_index), len(fields) - 1))
    field = fields[index]
    written = format_param_value(str(field.get("spec") or field.get("label") or ""), value)
    start = int(field.get("start") or 0)
    end = int(field.get("end") or start)
    if parsed.get("kind") == "call" and not str(field.get("raw") or "") and start == parsed.get("close"):
        open_index = int(parsed.get("open") or 0)
        close_index = int(parsed.get("close") or 0)
        inside = line[open_index + 1 : close_index]
        existing = [span[2] for span in _split_args_spans(line, open_index + 1, close_index)]
        while len(existing) < index:
            missing = fields[len(existing)]
            filler = str(missing.get("spec") or missing.get("label") or "").strip() or "值"
            existing.append(filler)
        existing.append(written)
        return f"{line[: open_index + 1]}{', '.join(existing)}{line[close_index:]}"
    return line[:start] + written + line[end:]


def align_block_keyword_line(line: str, source: str, line_index: int) -> Optional[str]:
    stripped = str(line or "").strip()
    if not stripped.endswith(":"):
        return None
    name = command_name_from_snippet(stripped)
    if not _is_outdent_keyword(name):
        return None
    lines = source_lines(source)
    safe_index = max(0, min(int(line_index), len(lines) - 1))
    aligned = indent_snippet(stripped, _outer_indent(lines, safe_index))
    return aligned if aligned != line else None


def plan_command_insert(
    item: Dict[str, Any],
    source: str,
    line_index: int = 0,
    column: Optional[int] = None,
    select_start: Optional[int] = None,
    select_end: Optional[int] = None,
) -> Dict[str, Any]:
    snippet = str((item or {}).get("snippet") or "").rstrip()
    name = str((item or {}).get("name") or command_name_from_snippet(snippet)).strip()
    return plan_snippet_insert(snippet, source, line_index, column, select_start, select_end, name=name)


def _replace_span(line: str, start: int, end: int, snippet: str, line_index: int) -> Dict[str, Any]:
    return {
        "mode": "replace",
        "line": line_index,
        "text": line[:start] + snippet + line[end:],
    }


def plan_snippet_insert(
    snippet: str,
    source: str,
    line_index: int = 0,
    column: Optional[int] = None,
    select_start: Optional[int] = None,
    select_end: Optional[int] = None,
    name: Optional[str] = None,
) -> Dict[str, Any]:
    """易语言式插入：有返回值就填进当前参数；选中已有值则包成第一个参数；否则另起一行。"""
    snippet = str(snippet or "").rstrip()
    lines = source_lines(source)
    safe_index = max(0, min(int(line_index), len(lines) - 1))
    current = lines[safe_index]
    command = str(name or command_name_from_snippet(snippet)).strip()
    if not _is_concrete_snippet(snippet):
        snippet = catalog_snippet(command) or snippet
    result_var = _nearest_result_var(lines, safe_index)
    cursor_column = len(current) if column is None else max(0, min(int(column), len(current)))
    start, end = ident_span(current, cursor_column)
    token = current[start:end]
    statement = statement_for_insert(command, snippet, result_var, current)

    if _should_complete_token(token, command, current):
        return _replace_span(current, start, end, statement, safe_index)

    can_nest = command_kind(command) == "expr"
    target = _nest_target(current, cursor_column, select_start, select_end) if can_nest else None
    if target:
        selected = current[target[0] : target[1]].strip()
        fill = wrap_first_arg(snippet, selected) if selected and not is_placeholder_text(selected) else snippet
        return _replace_span(current, target[0], target[1], fill, safe_index)

    if not current.strip():
        indent = _indent_from_above(lines, safe_index) or leading_whitespace(current)
        if _is_outdent_keyword(command):
            indent = _outer_indent(lines, safe_index)
        return {
            "mode": "replace",
            "line": safe_index,
            "text": indent_snippet(statement, indent),
        }
    if _is_block_header(current):
        indent = leading_whitespace(current)
        if not _is_outdent_keyword(command):
            indent += "    "
        return {
            "mode": "after",
            "line": _block_body_end(lines, safe_index),
            "text": "\n" + indent_snippet(statement, indent),
        }
    indent = leading_whitespace(current)
    if _is_outdent_keyword(command):
        indent = _outer_indent(lines, safe_index)
    return {
        "mode": "after",
        "line": safe_index,
        "text": "\n" + indent_snippet(statement, indent),
    }

_COMMAND_EXAMPLES = {
    "找图": (
        '图 = 找图("确定.png", 阈值=0.8)\n'
        "如果 图:\n"
        "    记录(图.分数)\n"
        "    点击(图)\n"
        "否则:\n"
        '    失败("没找到确定")\n'
        "\n"
        '图 = 找图("确定.png", "确定亮.png", 阈值=0.8)\n'
        '图 = 找图("确定.png", 区域=(10, 20, 200, 80))'
    ),
    "点击": (
        '图 = 找图("确定.png")\n'
        "如果 图:\n"
        "    点击(图)\n"
        '    点击(图, 键="右键")\n'
        "点击(120, 80)\n"
        "点击()"
    ),
    "移动": (
        '图 = 找图("确定.png")\n'
        "如果 图:\n"
        "    移动(图)\n"
        "移动(120, 80)"
    ),
    "按键": (
        '按键("Enter")\n'
        '按键("W", 秒=0.4, 连发=真)\n'
        '按键("W", 秒=0.4, 连发=真, 间隔=0.05)\n'
        '按键("W", 秒=0.4, 连发=假)\n'
        '按键("W", 秒=(0.2, 0.6), 连发=真)\n'
        '按键("Ctrl+C")'
    ),
    "输入": '输入("你好")',
    "延时": "延时(0.3)",
    "找字": (
        '字 = 找字(目标="金币")\n'
        "如果 字:\n"
        "    记录(字.内容)\n"
        "    点文字(\"金币\")"
    ),
    "找字库": (
        '字 = 找字库(目标="确定", 字库="ui.txt")\n'
        "如果 字:\n"
        "    记录(字.内容)\n"
        "    点字库(\"确定\", 字库=\"ui.txt\")"
    ),
    "点字库": '点字库("确定", 字库="ui.txt")',
    "等字库": '字 = 等字库(目标="确定", 字库="ui.txt", 超时=8)',
    "等字库消失": '等字库消失(目标="确定", 字库="ui.txt", 超时=8)',
    "检测": (
        '结果 = 检测("yolo/xxx.onnx", 类别="敌人")\n'
        "如果 结果:\n"
        "    记录(结果.类别)\n"
        "    记录(结果.分数)\n"
        "    点击(结果)\n"
        "\n"
        "循环 目标 在 结果.列表:\n"
        '    如果 目标.类别 是 "敌人":\n'
        "        点击(目标)\n"
        "        中断"
    ),
    "框内点": (
        '结果 = 检测("yolo/xxx.onnx", 类别="敌人")\n'
        "如果 结果:\n"
        "    点 = 框内点(结果, 0.5, 0.85)\n"
        "    点击(点)"
    ),
    "随机点": (
        '结果 = 检测("yolo/xxx.onnx", 类别="敌人")\n'
        "如果 结果:\n"
        "    点击(随机点(结果))"
    ),
    "找色": (
        '色 = 找色("255,0,0")\n'
        "如果 色:\n"
        "    点击(色)"
    ),
    "拖拽": (
        "拖拽(10, 20, 200, 80)\n"
        '结果 = 检测("yolo/xxx.onnx", 类别="敌人")\n'
        "如果 结果:\n"
        "    拖拽(结果, 框内点(结果, 0.1, 0.5))"
    ),
    "滚轮": (
        '滚轮(方向="向下", 步数=3)\n'
        '结果 = 检测("yolo/xxx.onnx")\n'
        "如果 结果:\n"
        '    滚轮(结果, 方向="向下", 步数=3)'
    ),
    "点文字": (
        '点文字("确定")\n'
        '点文字("确定", 区域=(10, 20, 200, 40))'
    ),
    "点元素": '点元素("确定")',
    "等图": (
        '图 = 等图("确定.png", 超时=8)\n'
        "如果 图:\n"
        "    点击(图)"
    ),
    "等色": (
        '色 = 等色("255,0,0", 超时=8)\n'
        "如果 色:\n"
        "    点击(色)"
    ),
    "等文字": (
        '字 = 等文字("金币", 超时=8)\n'
        "如果 字:\n"
        '    点文字("金币")'
    ),
    "等图消失": '等图消失("加载中.png", 超时=8)',
    "等色消失": '等色消失("255,0,0", 超时=8)',
    "等文字消失": '等文字消失("请稍候", 超时=8)',
    "等检测": (
        '结果 = 等检测("yolo/xxx.onnx", 类别="敌人", 超时=8)\n'
        "如果 结果:\n"
        "    点击(结果)"
    ),
    "等检测消失": '等检测消失("yolo/xxx.onnx", 类别="敌人", 超时=8)',
    "持续找图": (
        '持续找图("确定.png", 间隔=0.3)\n'
        "当 真:\n"
        "    如果 找图:\n"
        "        点击(找图)\n"
        "        中断\n"
        "    延时(0.05)"
    ),
    "停止找图": "停止找图()",
    "停止检测": "停止检测()",
    "找所有图": (
        '图 = 找所有图("怪.png", 阈值=0.8, 最多=20)\n'
        "循环 一项 在 图.列表:\n"
        "    记录(一项.x)\n"
        "    点击(一项)"
    ),
    "取色": (
        "色 = 取色(120, 80)\n"
        "记录(色.红)\n"
        "记录(色.绿)\n"
        "记录(色.蓝)"
    ),
    "比色": (
        '如果 比色(120, 80, "255,0,0", 偏色=20):\n'
        '    记录("颜色对上了")'
    ),
    "按下": (
        '按下("W")\n'
        "延时(0.4)\n"
        '松开("W")'
    ),
    "松开": '松开("W")',
    "按住": (
        '按住("W", 秒=0.4)\n'
        "按住(目标, 秒=(0.2, 0.6))"
    ),
    "连点": (
        '图 = 找图("确定.png")\n'
        "如果 图:\n"
        "    连点(图, 次数=3, 间隔=0.08)"
    ),
    "等毫秒": "等毫秒(50)",
    "鼠标位置": (
        "位置 = 鼠标位置()\n"
        "记录(位置.x)\n"
        "记录(位置.y)"
    ),
    "相对移动": "相对移动(10, 0)",
    "窗口.激活": "窗口.激活()",
    "窗口.查找元素": "元素 = 窗口.查找元素(名称=\"确定\")\n记录(元素.列表)",
    "窗口.元素文本": "文字 = 窗口.元素文本(名称=\"状态\")\n记录(文字.内容)",
    "窗口.设置元素值": "窗口.设置元素值(\"内容\", 名称=\"输入框\")",
    "窗口.元素属性": "属性 = 窗口.元素属性(名称=\"确定\")\n记录(属性.控件类型)",
    "窗口.元素聚焦": "窗口.元素聚焦(名称=\"输入框\")",
    "窗口.元素切换": "窗口.元素切换(名称=\"记住我\", 状态=真)",
    "窗口.元素选择": "窗口.元素选择(值=\"选项一\", 控件类型=\"下拉框\")",
    "窗口.元素展开": "窗口.元素展开(名称=\"项目\")",
    "窗口.元素折叠": "窗口.元素折叠(名称=\"项目\")",
    "窗口.子窗口": "子项 = 窗口.子窗口()\n记录(长度(子项))",
    "窗口.消息": "窗口.消息(1, 0, 投递=真)",
    "窗口.Z序": "列表 = 窗口.Z序()\n记录(长度(列表))",
    "窗口.DPI": "dpi = 窗口.DPI()\n记录(dpi.dpi)",
    "网络.请求": "结果 = 网络.请求(\"https://example.com\")\n记录(结果.状态)",
    "窗口.设置分辨率": (
        "窗口.设置分辨率(1280, 720)\n"
        '窗口.设置分辨率(1280, 720, "全部")\n'
        '窗口.设置分辨率("阴阳师", 报错=假)'
    ),
    "播放": (
        '播放("提示.wav")\n'
        '播放("提示.mp3", 等待=假)\n'
        "延时(0.5)\n"
        "停止播放()"
    ),
    "停止播放": "停止播放()",
    "回放": (
        '回放("replays/过图.replay.json", 速度=1)\n'
        '回放("replays/过图.replay.json", 速度=1, 次数=1, 等待=假)\n'
        "暂停回放()\n"
        "继续回放()\n"
        "停止回放()"
    ),
    "暂停回放": "暂停回放()",
    "继续回放": "继续回放()",
    "停止回放": "停止回放()",
    "截图": (
        '图 = 截图("shot.png")\n'
        "记录(图.路径)"
    ),
    "组件.加载": (
        '插件 = 组件.加载("python", "math")\n'
        '结果 = 组件.调用(插件, "sqrt", 81)\n'
        "记录(结果.返回值)"
    ),
    "组件.调用": (
        '插件 = 组件.加载("dll", "plugins/example.dll")\n'
        '结果 = 组件.调用(插件, "Add", 2, 3, 参数类型=["int32", "int32"], 返回类型="int32")'
    ),
    "组件.读取": '版本 = 组件.读取(插件, "Version")',
    "组件.写入": '组件.写入(插件, "Enabled", 真)',
    "组件.运行": (
        '结果 = 组件.运行("tools/helper.exe", "--version", 超时=30)\n'
        "记录(结果.标准输出)"
    ),
    "组件.关闭": "组件.关闭(插件)",
    "等按键": (
        '如果 等按键("F1", 超时=8):\n'
        "    点击()"
    ),
    "记录": '记录("已点确定")',
    "成功": "成功()",
    "失败": '失败("没找到确定")',
    "变量.获取": (
        '次数 = 变量.获取("次数", 0)\n'
        "如果 次数 >= 3:\n"
        '    失败("重试过多")'
    ),
    "变量.设置": '变量.设置("次数", 0)',
    "变量.增加": (
        '变量.设置("次数", 0)\n'
        '变量.增加("次数")\n'
        '记录(变量.获取("次数", 0))'
    ),
    "变量.全局获取": (
        '次数 = 变量.全局获取("次数", 0)\n'
        "如果 次数 >= 3:\n"
        '    失败("重试过多")'
    ),
    "变量.全局设置": '变量.全局设置("次数", 0)',
    "变量.全局增加": (
        '变量.全局设置("次数", 0)\n'
        '变量.全局增加("次数")\n'
        '记录(变量.全局获取("次数", 0))'
    ),
    "区域.设置": (
        '区域.设置("打怪", 80, 120, 640, 360)\n'
        '持续检测("yolo/怪.onnx", 类别="怪物", 区域="打怪", 间隔=0.2)\n'
        "当 真:\n"
        "    如果 检测:\n"
        "        点击(检测)\n"
        '        按键("1")\n'
        "    延时(0.05)"
    ),
    "区域.获取": (
        '区 = 区域.获取("打怪")\n'
        "如果 区:\n"
        "    记录(区.宽)"
    ),
    "区域.存在": '如果 区域.存在("打怪"):\n    记录("已登记")',
    "区域.删除": '区域.删除("打怪")',
    "区域.列表": "记录(区域.列表())",
    "剪贴板.获取": (
        "文本 = 剪贴板.获取()\n"
        "记录(文本)"
    ),
    "剪贴板.设置": '剪贴板.设置("已复制")',
    "上次": (
        '找图("确定.png")\n'
        "如果 上次:\n"
        "    记录(上次.分数)\n"
        "    点击(上次.x, 上次.y)"
    ),
    "文字": (
        '找字(目标="金币")\n'
        "如果 文字:\n"
        "    记录(文字.内容)"
    ),
    "卡片": (
        "如果 卡片[3].通过:\n"
        "    记录(卡片[3].内容)"
    ),
    "窗口.宽": "记录(窗口.宽)",
    "窗口.高": "记录(窗口.高)",
    "如果": (
        '图 = 找图("确定.png")\n'
        "如果 图:\n"
        "    点击(图)\n"
        "否则:\n"
        '    失败("没找到确定")'
    ),
    "否则如果": (
        '字 = 找字(目标="金币")\n'
        '如果 字.内容 是 "金币":\n'
        '    点文字("金币")\n'
        '否则如果 字.内容 是 "银币":\n'
        '    点文字("银币")\n'
        "否则:\n"
        '    记录("都不是")'
    ),
    "否则": (
        '图 = 找图("确定.png")\n'
        "如果 图:\n"
        "    点击(图)\n"
        "否则:\n"
        '    失败("没找到确定")'
    ),
    "循环": (
        "循环 i 在 范围(6):\n"
        "    记录(i)"
    ),
    "当": (
        "当 找图:\n"
        "    点击(找图)\n"
        "    延时(0.3)"
    ),
    "中断": (
        "循环 i 在 范围(10):\n"
        '    如果 找图("确定.png"):\n'
        "        中断"
    ),
    "继续": (
        "循环 i 在 范围(10):\n"
        "    如果 i 是 0:\n"
        "        继续\n"
        "    记录(i)"
    ),
    "子程序": (
        "子程序 点确定():\n"
        '    图 = 找图("确定.png")\n'
        "    如果 图:\n"
        "        点击(图)\n"
        "        返回 真\n"
        "    返回 假\n"
        "\n"
        "如果 点确定():\n"
        "    成功()"
    ),
    "长度": '记录(长度(检测.列表))',
    "整数": "记录(整数(3.9))",
    "小数": "记录(小数(\"3.14\"))",
    "到文本": "记录(到文本(12))",
    "真假": "如果 真假(图):\n    点击(图)",
    "最小": "记录(最小(3, 8))",
    "最大": "记录(最大(3, 8))",
    "绝对值": "记录(绝对值(-4))",
    "开方": (
        "差x = 目标.x - 自己.x\n"
        "差y = 目标.y - 自己.y\n"
        "记录(开方(差x * 差x + 差y * 差y))"
    ),
    "限制": "x = 限制(目标.x, 0, 窗口.宽)",
    "距离": (
        '自己 = 检测("yolo/xxx.onnx", 类别="自己")\n'
        '目标 = 检测("yolo/xxx.onnx", 类别="敌人")\n'
        "如果 自己 并且 目标:\n"
        "    记录(距离(自己, 目标))"
    ),
    "角度": (
        '自己 = 检测("yolo/xxx.onnx", 类别="自己")\n'
        '目标 = 检测("yolo/xxx.onnx", 类别="敌人")\n'
        "如果 自己 并且 目标:\n"
        "    记录(角度(自己, 目标))"
    ),
    "正弦": "走y = 自己.y + 正弦(角) * 80",
    "余弦": "走x = 自己.x + 余弦(角) * 80",
    "范围": (
        "循环 i 在 范围(6):\n"
        "    记录(i)\n"
        "循环 n 在 范围(1, 5):\n"
        "    记录(n)"
    ),
    "随机": (
        "记录(随机())\n"
        "记录(随机(10))\n"
        "记录(随机(1, 10))\n"
        '按住("W", 秒=随机(0.2, 0.6))'
    ),
    "包含": (
        '如果 包含(文字.内容, "金币"):\n'
        '    点文字("金币")'
    ),
    "截取": '记录(截取(文字.内容, 1, 2))',
    "替换": '记录(替换(文字.内容, "旧", "新"))',
    "分割": (
        '项 = 分割("1,2,3", ",")\n'
        "记录(项[0])"
    ),
    "时间": (
        "起 = 时间()\n"
        "延时(0.3)\n"
        "记录(时间() - 起)"
    ),
    "去空格": '记录(去空格("  金币  "))',
    "查找": (
        '位置 = 查找(文字.内容, "金币")\n'
        "如果 位置 > 0:\n"
        "    记录(位置)"
    ),
    "提取数字": (
        '数量 = 提取数字(文字.内容)\n'
        "记录(数量)"
    ),
}
_COMMAND_RETURNS = {
    "找图": "赋给变量后，如果 图: 表示找到了。可读 图.分数  图.路径  图.x  图.y  图.通过",
    "等图": "找到就返回。超时则失败。可读 图.分数  图.路径  图.x  图.y",
    "等图消失": "图消失了为真。超时则失败。",
    "持续找图": "后台更新 找图。后面读 找图、找图.分数、找图.x，不要再写 找图()",
    "停止找图": "无",
    "找所有图": "图.列表 是全部匹配，图[0] 是第一个。也可读 图.x  图.y  图.通过",
    "找字": "如果 字: 表示认出了。可读 字.内容  字.x  字.y  字.通过",
    "找字库": "如果 字: 表示字库认出了。可读 字.内容  字.x  字.y  字.通过",
    "等字库": "找到就返回。可读 字.内容  字.x  字.y",
    "等字库消失": "这段字认不到了为真。",
    "点字库": "先用字库找再点。可读 结果.内容  结果.x  结果.y  结果.通过",
    "等文字": "找到就返回。可读 字.内容  字.x  字.y",
    "等文字消失": "这段字认不到了为真。",
    "点文字": "先找再点。可读 结果.内容  结果.x  结果.y  结果.通过",
    "点元素": "可读 结果.通过",
    "找色": "如果 色: 表示找到了。可读 色.颜色  色.红  色.绿  色.蓝  色.x  色.y",
    "等色": "找到就返回。可读 色.颜色  色.x  色.y",
    "等色消失": "这个颜色找不到了为真。",
    "取色": "色.红  色.绿  色.蓝  色.颜色",
    "比色": "颜色对上了为真。",
    "检测": "只认这一帧，默认不点。可读 结果.类别  结果.分数  结果.列表  结果.x  结果.y  结果.宽  结果.高",
    "等检测": "出现就返回。可读 结果.类别  结果.列表  结果.x  结果.y",
    "等检测消失": "这个类别检测不到了为真。",
    "持续检测": "结果写进 检测。读 检测、检测.列表，不要再写 检测()",
    "停止检测": "无",
    "多线程": "启动受控子程序线程，返回线程名",
    "关闭线程": "请求停止指定线程；省略名字时停止本脚本全部线程",
    "线程状态": "可读 状态.状态、状态.运行、状态.结果、状态.错误；省略名字时返回 状态.列表",
    "等待线程": "等待线程结束并返回 状态、结果、错误；超时后仍可继续查询",
    "线程结果": "读取线程最近结果，不等待；可读 结果.状态、结果.结果、结果.错误",
    "点击": "可读 结果.x  结果.y。不写参数时点上次成功位置",
    "移动": "无",
    "框内点": "返回一个点，给 点击、移动、拖拽 用",
    "随机点": "返回框内随机一点",
    "拖拽": "无",
    "滚轮": "无",
    "按键": "无",
    "输入": "无",
    "延时": "无",
    "等毫秒": "无",
    "按下": "无",
    "松开": "无",
    "按住": "无",
    "连点": "无",
    "鼠标位置": "位置.x  位置.y",
    "相对移动": "无",
    "窗口.激活": "无",
    "窗口.查找元素": "可读 结果.列表；每项含名称、自动化ID、类名、控件类型、x、y、宽、高",
    "窗口.元素文本": "可读 结果.内容  结果.通过",
    "窗口.设置元素值": "可读 结果.通过  结果.值",
    "窗口.元素属性": "可读 结果.通过、结果.名称、结果.控件类型、结果.启用、结果.聚焦、结果.x、结果.y、结果.宽、结果.高",
    "窗口.元素聚焦": "可读 结果.通过",
    "窗口.元素切换": "可读 结果.通过、结果.勾选、结果.状态",
    "窗口.元素选择": "可读 结果.通过、结果.值",
    "窗口.元素展开": "可读 结果.通过、结果.展开",
    "窗口.元素折叠": "可读 结果.通过、结果.展开",
    "窗口.子窗口": "返回子窗口列表和层级",
    "窗口.消息": "可读 结果.消息  结果.投递  结果.通过",
    "窗口.Z序": "返回按 z_index 排序的顶级窗口列表",
    "窗口.DPI": "可读 结果.dpi  结果.scale",
    "网络.请求": "可读 结果.状态  结果.头  结果.内容  结果.字节数  结果.地址",
    "事件.等待窗口": "窗口出现时返回窗口信息，超时返回假",
    "事件.等待进程": "进程运行时返回进程信息，超时返回假",
    "播放": "无。等待=真 时播完才返回",
    "停止播放": "无",
    "记录": "无",
    "成功": "结束脚本，走成功线",
    "失败": "结束脚本，走失败线",
    "上次": "上次.通过  上次.成功  上次.类型  上次.分数  上次.阈值  上次.x  上次.y",
    "文字": "文字.内容  文字.通过",
    "卡片": "卡片[编号].内容  卡片[编号].分数  卡片[编号].通过",
    "窗口.宽": "当前绑定窗口客户区宽度",
    "窗口.高": "当前绑定窗口客户区高度",
    "窗口.设置分辨率": "可读 结果.宽  结果.高  结果.通过。报错=假 时失败返回假",
    "回放": "无。等待=真 时播完才返回；等待=假 时立刻返回",
    "暂停回放": "可读 结果.通过。没有回放时为假",
    "继续回放": "可读 结果.通过。没有暂停的回放时为假",
    "停止回放": "无。没有回放时也算成功",
    "截图": "可读 图.路径  图.通过",
    "组件.加载": "组件句柄。可直接传给 组件.调用、组件.读取、组件.写入、组件.关闭",
    "组件.调用": "可读 结果.返回值  结果.通过；运行进程时还可读退出码和标准输出/错误",
    "组件.读取": "可读 结果.返回值  结果.通过",
    "组件.写入": "可读 结果.返回值  结果.通过",
    "组件.运行": "可读 结果.退出码  结果.标准输出  结果.标准错误  结果.进程号  结果.通过",
    "组件.关闭": "关闭数量和通过状态",
    "等按键": "按时为真。超时为假",
    "变量.获取": "返回这个名字的值",
    "变量.设置": "无",
    "变量.增加": "返回增加后的值",
    "变量.全局获取": "返回这个名字的全局值",
    "变量.全局设置": "无",
    "变量.全局增加": "返回增加后的值",
    "区域.设置": "可读 区.x 区.y 区.宽 区.高",
    "区域.获取": "可读 区.x 区.y 区.宽 区.高；没有则 通过=假",
    "区域.存在": "已登记为真",
    "区域.删除": "删除成功时为真",
    "区域.列表": "返回本卡已登记区域",
    "剪贴板.获取": "返回当前剪贴板文字",
    "剪贴板.设置": "无",
    "多线程": "无。子程序自己在旁边跑",
    "关闭线程": "无",
    "如果": "无。条件成立才跑缩进里的命令",
    "否则如果": "无",
    "否则": "无",
    "循环": "无",
    "当": "无",
    "中断": "无",
    "继续": "无",
    "子程序": "子程序里 返回 值 会带回给调用的地方",
    "长度": "返回个数",
    "整数": "返回整数",
    "小数": "返回小数",
    "到文本": "返回文字",
    "真假": "返回 真 或 假",
    "最小": "返回较小的那个",
    "最大": "返回较大的那个",
    "绝对值": "返回去掉正负号后的数",
    "开方": "返回平方根",
    "限制": "返回夹在两头之间的数",
    "距离": "返回像素距离",
    "角度": "返回度数",
    "正弦": "返回正弦值",
    "余弦": "返回余弦值",
    "范围": "返回一串数字，给 循环 用",
    "随机": "返回随机数",
    "包含": "有这段字为真",
    "截取": "返回截出来的文字",
    "替换": "返回换过的文字",
    "分割": "返回列表",
    "时间": "返回当前毫秒",
    "去空格": "返回去掉头尾空白的文字",
    "查找": "找到返回位置（从 1 起），找不到是 0",
    "提取数字": "返回抽出的第一个数字，没有是 0",
    "变量.存在": "变量存在时为真",
    "变量.删除": "删除成功时为真",
    "变量.全局存在": "全局变量存在时为真",
    "变量.全局删除": "删除成功时为真",
    "数据.读取": "返回文本；文件不存在时返回默认值",
    "数据.写入": "写入成功时为真",
    "数据.JSON读取": "返回解析后的对象；文件不存在或 JSON 无效时返回默认值",
    "数据.JSON写入": "写入成功时为真",
    "数据.存在": "文件存在时为真",
    "数据.删除": "删除成功时为真",
    "JSON解析": "返回列表或字典",
    "JSON生成": "返回 JSON 文本",
    "字典获取": "返回键对应值或默认值",
    "字典设置": "返回写入后的字典",
    "字典键": "返回键列表",
    "字典值": "返回值列表",
    "列表追加": "返回追加后的列表",
    "列表合并": "返回合并后的列表",
    "列表弹出": "返回被移除的项目",
    "类型": "返回类型名称",
    "断言": "条件不成立时报告脚本错误",
    "开头是": "满足前缀时为真",
    "结尾是": "满足后缀时为真",
    "枚举": "返回序号和项目的迭代器",
    "配对": "返回按位置配对的迭代器",
    "排序": "返回排序后的新列表",
    "四舍五入": "返回四舍五入后的数值",
    "全部": "全部为真时为真",
    "任一": "至少一项为真时为真",
    "平方根": "返回平方根",
    "函数": "定义可复用函数",
    "返回": "从函数返回值",
    "略过": "跳过当前循环迭代",
}
_EXTRA_PARAM_HELP = {
    "文件": "用资源栏导入，或写成 \"提示.wav\"",
    "等待": "写成 真 或 假。真是播完再往下，假是接着跑",
    "点击": "写成 真 或 假。真表示找到后立刻点",
    "区域": "用工具栏「框选区域」写入 区域.设置，或写成 (x, y, 宽, 高) / 区域名",
    "图片2": "第二张图。多张图并行识别，按顺序取第一张命中，阈值仍写 阈值=",
    "最多": "最多返回多少个匹配",
    "超时": "最多等多少秒",
    "间隔": "写成秒数，例如 0.05。按键连发时是两次出字的间隔；查找命令里是两次查找之间的等待",
    "偏色": "每个颜色通道允许差多少",
    "步长": "每次加多少，默认 1",
    "毫秒": "等待的毫秒数",
    "按W": "你自己的子程序名，不要加括号",
    "默认值": "没有这个名字时用这个值",
    "模式": "可写 前台驱动、前台脚本、后台消息、后台投递；省略时沿用线程模式",
    "值": "要写入或转换的值",
    "步数": "滚轮滚几格",
    "次数": "重复多少次",
}


def split_command_signatures(signature: str) -> List[str]:
    text = str(signature or "").strip()
    if not text:
        return []
    parts = [part.strip() for part in text.replace("\n", " / ").split(" / ") if part.strip()]
    return parts or [text]


def _param_help_map() -> Dict[str, str]:
    hints = dict(_EXTRA_PARAM_HELP)
    try:
        from task_workflow.script_sandbox import _PLACEHOLDER_HINTS

        merged = dict(_PLACEHOLDER_HINTS)
        merged.update(hints)
        return merged
    except Exception:
        return hints


def _command_param_lines(item: Dict[str, Any]) -> List[str]:
    hints = _param_help_map()
    hints.update(
        {str(key): str(value) for key, value in dict((item or {}).get("param_help") or {}).items()}
    )
    lines = []
    seen = set()
    for overload in _normalize_overloads((item or {}).get("params")):
        for spec in overload:
            label = _field_label(spec)
            if not label or label in seen:
                continue
            seen.add(label)
            hint = str(hints.get(label) or "").strip()
            default = _field_default(spec)
            if hint and default:
                lines.append(f"{label}  {hint}。默认 {default}")
            elif hint:
                lines.append(f"{label}  {hint}")
            elif default:
                lines.append(f"{label}  默认 {default}")
            else:
                lines.append(label)
    return lines


def command_example_text(item: Dict[str, Any]) -> str:
    example = str((item or {}).get("example") or "").rstrip()
    if example:
        return example
    name = str((item or {}).get("name") or "")
    example = str(_COMMAND_EXAMPLES.get(name) or "").rstrip()
    if example:
        return example
    return str((item or {}).get("snippet") or "").rstrip()


def command_help_text(item: Optional[Dict[str, Any]] = None) -> str:
    if not item:
        return "点列表里的命令，这里只显示这一条的写法和例子。"
    name = str(item.get("name") or "").strip()
    blocks = []
    signatures = split_command_signatures(str(item.get("signature") or ""))
    if not signatures and name:
        signatures = [name]
    if signatures:
        blocks.append("写法\n" + "\n".join(signatures))
    note = str(item.get("note") or "").strip()
    if note:
        blocks.append("说明\n" + note)
    params = _command_param_lines(item)
    if params:
        blocks.append("参数\n" + "\n".join(params))
    example = command_example_text(item)
    if example:
        blocks.append("例\n" + example)
    returns = str(_COMMAND_RETURNS.get(name) or "").strip()
    if not returns:
        returns = command_returns_label(name)
    blocks.append("返回\n" + returns)
    blocks.append("双击或回车插入。插入后不会选中参数。要填进某个参数，先点到那个格子再插入。")
    return "\n\n".join(block for block in blocks if block)


def get_params_definition() -> Dict[str, Dict[str, Any]]:
    return {
    "script_source": {
            "label": "命令",
            "type": "text",
            "multiline": True,
            "default": DEFAULT_SCRIPT_SOURCE,
            "placeholder": SCRIPT_PLACEHOLDER,
            "tooltip": "用中文命令调用找图、点击、按键等现有能力。",
        },
        "script_source_sha256": {
            "type": "hidden",
            "default": "",
            "description": "脚本源码完整性哈希，由编辑器自动维护。",
        },
        "allow_external_components": {
            "label": "允许外部组件",
            "type": "boolean",
            "default": False,
            "tooltip": "允许脚本运行外部程序并加载 Python、COM 或 DLL。只应对可信脚本启用。",
        },
    }


def validate_script_source(source: Any) -> None:
    from task_workflow.script_sandbox import ScriptError, unfilled_placeholder_error, validate_script

    text = str(source or "")
    validate_script(text)
    placeholder = unfilled_placeholder_error(text)
    if placeholder:
        raise ScriptError(placeholder)


def script_source_hash(source: Any) -> str:
    """Return a stable content digest used to detect modified script source."""
    return hashlib.sha256(str(source or "").encode("utf-8")).hexdigest()


def build_script_run_context(
    params: Dict[str, Any],
    source: str,
    counters: Dict[str, int],
    execution_mode: str = "foreground",
    target_hwnd: Optional[int] = None,
    window_region=None,
    card_id: Optional[int] = None,
    **kwargs,
) -> Dict[str, Any]:
    """构造 run_script 的 context，供正式执行与编辑器调试运行共用。

    含对 allow_external_components 的信任校验。
    """
    executor = kwargs.get("executor")
    context = {
        "counters": counters if isinstance(counters, dict) else {},
        "execution_mode": execution_mode,
        "target_hwnd": target_hwnd,
        "bound_windows": kwargs.get("bound_windows")
        if kwargs.get("bound_windows") is not None
        else getattr(executor, "bound_windows", None),
        "custom_width": kwargs.get("custom_width")
        if kwargs.get("custom_width") is not None
        else getattr(executor, "custom_width", 0),
        "custom_height": kwargs.get("custom_height")
        if kwargs.get("custom_height") is not None
        else getattr(executor, "custom_height", 0),
        "adjust_window_resolution": kwargs.get("adjust_window_resolution")
        or getattr(executor, "adjust_window_resolution", None),
        "window_region": window_region,
        "card_id": card_id,
        "get_image_data": kwargs.get("get_image_data"),
        "stop_checker": kwargs.get("stop_checker"),
        "pause_checker": kwargs.get("pause_checker"),
        "executor": executor,
        "debugger": kwargs.get("debugger"),
        "allow_external_components": False,
    }
    for key in ("images_dir", "sounds_dir", "dicts_dir", "yolo_dir", "replays_dir", "plugins_dir"):
        context[key] = kwargs.get(key) if kwargs.get(key) is not None else getattr(executor, key, None)
    if bool((params or {}).get("allow_external_components", False)):
        from task_workflow.external_component_trust import is_external_script_trusted

        context["allow_external_components"] = is_external_script_trusted(source)

    return context


def execute_task(
    params: Dict[str, Any],
    counters: Dict[str, int],
    execution_mode: str = "foreground",
    target_hwnd: Optional[int] = None,
    window_region=None,
    card_id: Optional[int] = None,
    **kwargs,
) -> Tuple[Any, ...]:
    source = str((params or {}).get("script_source") or "")
    if not source.strip():
        logger.error("[自定义脚本] 内容为空")
        return False, "执行下一步", None, "内容为空"

    expected_hash = str((params or {}).get("script_source_sha256") or "").strip().casefold()
    if expected_hash:
        actual_hash = script_source_hash(source)
        if expected_hash != actual_hash:
            logger.error("[自定义脚本] 内容完整性校验失败")
            return False, "执行下一步", None, "脚本内容完整性校验失败"

    try:
        from task_workflow.script_sandbox import ScriptError, run_script
        from task_workflow.workflow_context import get_runtime_store

        store = get_runtime_store()
        store.bind_counters(counters if isinstance(counters, dict) else {})
        store.set_current_card_id(card_id)
        context = build_script_run_context(
            params,
            source,
            counters=counters,
            execution_mode=execution_mode,
            target_hwnd=target_hwnd,
            window_region=window_region,
            card_id=card_id,
            bound_windows=kwargs.get("bound_windows"),
            custom_width=kwargs.get("custom_width"),
            custom_height=kwargs.get("custom_height"),
            adjust_window_resolution=kwargs.get("adjust_window_resolution"),
            get_image_data=kwargs.get("get_image_data"),
            stop_checker=kwargs.get("stop_checker"),
            pause_checker=kwargs.get("pause_checker"),
            executor=kwargs.get("executor"),
            debugger=kwargs.get("debugger"),
            images_dir=kwargs.get("images_dir"),
            sounds_dir=kwargs.get("sounds_dir"),
        )

        success, detail = run_script(
            source,
            store,
            logger,
            context=context,
        )
        if success:
            logger.info("[自定义脚本] 执行成功%s", f": {detail}" if detail else "")
            return True, "执行下一步", None
        logger.info("[自定义脚本] 执行失败: %s", detail or "判断失败")
        return False, "执行下一步", None, detail or "判断失败"
    except ScriptError as exc:
        logger.error("[自定义脚本] %s", exc)
        return False, "执行下一步", None, str(exc)
    except Exception as exc:
        logger.exception("[自定义脚本] 未预期错误: %s", exc)
        return False, "执行下一步", None, str(exc)
