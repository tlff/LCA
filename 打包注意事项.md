# LCA 打包注意事项（开发者参考）

本文档记录 LCA Windows 离线打包流程中踩过的环境/依赖坑，以及在 `build_assets/packaging/` 下各脚本里所做的对应修复。每次重新出包前请先扫一遍本文档。

---

## 1. 虚拟环境目录命名

打包脚本不再硬编码 `venv`，而是优先探测 `.venv`，回落到 `venv`。两个候选都要能跑 `Scripts\python.exe`。

### 涉及文件
- `build_assets/packaging/build_release.bat`（CHECK_VENV 段）
- `build_assets/packaging/stage_packaged_runtime_assets.py`（`--venv-site-packages` 参数 + `{site_packages}/...` 占位符）
- `app_core/runtime/worker_entry.py`（`resolve_project_venv_root` / `resolve_project_site_packages_dir` 是单一真相源）

### 规则
- 新建项目请使用 `.venv/`（更现代、与 uv 等工具默认一致）
- 旧仓库迁移过来时若用 `venv/`，打包脚本会自动找到
- 两者都没就 fail，错误信息明确

---

## 3. `requirements-runtime.txt`

这是 Nuitka 打包时**直接依赖真值**，不是普通的 pip 依赖列表。

### 关键点
- 文件由 `build_assets/packaging/generate_third_party_manifest.py` 读入，用于构建闭包
- **版本必须 = .venv 实际安装版本**，否则 verify_third_party_manifest.py 报"version drift"
- **只列直接依赖**，不要列传递依赖（如 `numpy` 是 `rapidocr` 的传递依赖，不该出现在这里）

### 重新生成方法（venv 升级后必须重做）
```
.venv\Scripts\python.exe -c "
import importlib.metadata as md
all_pkgs = {(d.metadata.get('Name') or '').strip().lower(): str(d.version) for d in md.distributions()}
# 从 requirements.txt 运行时段抽取直接依赖名（保持用户原始意图）
# 用 venv 实装版本号覆盖
"
```
或重跑 `build_assets\packaging\generate_third_party_manifest.py --output build_assets\third_party\manifest.json`（manifest.json 会重新生成完整清单）。

### 涉及文件
- `requirements-runtime.txt`（git 追踪，跨机器同步用）
- `build_assets/third_party/manifest.json`（本机生成，包含 native 组件 sha256）

---

## 4. Native 组件与运行时 DLL

打包需要以下 native 资产，**不在 git 里**，需要单独放：

| 资产 | 路径 | 来源 |
|---|---|---|
| AutoHotkey64.exe | `AutoHotkey/AutoHotkey64.exe` | AutoHotkey v1 官方 |
| interception.dll (x64) | `Interception/library/x64/interception.dll` | Interception 驱动项目 |
| interception.dll (x86) | `Interception/library/x86/interception.dll` | Interception 驱动项目 |
| IbInputSimulator.dll | `tools/ibinputsimulator/Binding.AHK2/IbInputSimulator.dll` | InputSimulator 项目（AHK 绑定）|
| ib_worker_core.ahk | `tools/ibinputsimulator/ib_worker_core.ahk` | 同上（必须含 `UIntP`，不含 `UInt*`）|

### PP-OCRv4 模型（不放 git）

| 文件 | sha256（必须严格匹配）|
|---|---|
| `models/rapidocr/ch_PP-OCRv4_det_mobile.onnx` | `d2a7720d45a54257208b1e13e36a8479894cb74155a5efe29462512d42f49da9` |
| `models/rapidocr/ch_ppocr_mobile_v2.0_cls_mobile.onnx` | `e47acedf663230f8863ff1ab0e64dd2d82b838fceb5957146dab185a89d6215c` |
| `models/rapidocr/ch_PP-OCRv4_rec_mobile.onnx` | `48fc40f24f6d2a207a2b1091d3437eb3cc3eb6b676dc3ef9c37384005483683b` |

sha256 校验写死在 `services/ocr_runtime_contract.py:12-25`。文件缺失或 hash 不对，Nuitka 阶段直接 fail。

### 涉及文件
- `services/ocr_runtime_contract.py`（模型清单真值）

---

## 5. Nuitka 命令行

### 必须加 `--assume-yes-for-downloads`

BAT 是非交互环境，Nuitka 编译时会在最后 DLL 依赖扫描阶段问"是否下载 Dependency Walker"，默认拒绝会让 standalone 模式报：

```
FATAL: Nuitka does not work in --standalone or --onefile on Windows without.
```

加了 `--assume-yes-for-downloads` 后会自动从 `https://dependencywalker.com/depends22_x64.zip` 下载 depends.exe 并缓存到 `%LOCALAPPDATA%\Nuitka\Nuitka\Cache\depends\x86_64\`。

### 涉及文件
- `build_assets/packaging/run_nuitka_main_build.py:402-408`（命令构建列表）

### clcache 加速

首次 Nuitka 编译 5-10 分钟；再次编译因 clcache 全量命中只需约 2 分钟。缓存目录：`%LOCALAPPDATA%\Nuitka\Nuitka\Cache\clcache\`。清掉它就强制重编译。

---

## 6. Inno Setup 安装路径

### BAT 探测路径
`build_assets/packaging/build_release.bat:296-300` 探测 4 个候选：

1. `C:\Program Files (x86)\Inno Setup 6\ISCC.exe`
2. `C:\Program Files\Inno Setup 6\ISCC.exe`
3. `D:\Inno Setup 6\ISCC.exe`
4. `D:\app\Scoop\apps\inno-setup\current\ISCC.exe`（Scoop 用户）

装到其它路径请自己加进列表。

### Scoop 版 Inno Setup 缺中文语言包

Scoop 装的 Inno Setup 是精简版，`6.7.3\Languages\` 下**没有** `ChineseSimplified.isl`，ISCC 编译时会报错：

```
Error: Couldn't open include file "D:\app\Scoop\apps\inno-setup\current\Languages\ChineseSimplified.isl"
```

**手动补齐**：从官方 jrsoftware/issrc GitHub 仓库下载 ChineseSimplified.isl，放到 `D:\app\Scoop\apps\inno-setup\6.7.3\Languages\`。

```powershell
Invoke-WebRequest -Uri "https://raw.githubusercontent.com/jrsoftware/issrc/main/Files/Languages/ChineseSimplified.isl" -OutFile "D:\app\Scoop\apps\inno-setup\6.7.3\Languages\ChineseSimplified.isl"
```

或者把 `build_assets/packaging/setup.iss:98` 改为：
```ini
Name: "english"; MessagesFile: "compiler:Default.isl"
```
（用英文安装向导）

---

## 7. 打包模式

### 完整构建（重编译 EXE + 打安装包）
```
build_assets\packaging\build_release.bat
```
耗时 10-15 分钟（含 Nuitka 编译 + Inno Setup）。

### 只打安装包（复用现有 EXE）
```
set LCA_INSTALLER_ONLY=1
build_assets\packaging\build_release.bat
```
前提：`build_assets/packaging/build_output/main.dist/main.exe` 必须已存在。耗时 2-3 分钟。

### 非交互模式
```
set LCA_NONINTERACTIVE=1
build_assets\packaging\build_release.bat
```
跳过错末尾的 `pause`。

---

## 8. 常见失败排查

| 报错 | 原因 | 修法 |
|---|---|---|
| `Missing virtual environment under F:\LCA` | `.venv`/`venv` 都没创建 | 创建 `.venv` 并装依赖 |
| `Third-party manifest verification failed`（组件缺失）| native 资产没放齐 | 补齐 AutoHotkey / Interception / IbInputSimulator |
| `Third-party manifest verification failed`（版本漂移）| `requirements-runtime.txt` 与 venv 不一致 | 重跑 generate_third_party_manifest.py |
| `Missing required Nuitka build inputs: models/rapidocr` | OCR 模型缺失 | 补齐 models/rapidocr/ 下 3 个 ONNX |
| `FATAL: Nuitka does not work in --standalone or --onefile on Windows without.` | Dependency Walker 下载被拒 | 已修：run_nuitka_main_build.py 加 `--assume-yes-for-downloads` |
| `Missing source UIAutomationClient_VC140_X64.dll` | venv 没装 uiautomation 包 | `pip install uiautomation` |
| `Inno Setup not found` | ISCC 不在探测路径 | 装 Inno Setup 或把 ISCC 路径加到 build_release.bat |
| `Couldn't open include file "D:\app\Scoop\apps\inno-setup\current\Languages\ChineseSimplified.isl"` | Scoop 版缺中文 | 见上方第 6 节 |

---

## 9. 流程图/工作流改动

与打包无关但本仓库涉及到的注意点：

- 流程图节点的 sequential 输出端口支持**一拖多**（方案 A3，按声明顺序串行执行，每个后续节点独立判定 success/failure）。修改见 `ui/workflow_parts/workflow_view_connection_mixin.py`、`task_workflow/executor.py`、`task_workflow/graph_engine.py`。